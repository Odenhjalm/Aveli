# auth — extracted tasks

## blocking
- None.

## important
- `auth_refresh_canonical_contract_docs`: rewrite the auth baseline so forgot/reset/verification flows are described from current mounted code, not from the old unmounted-router audit.
- `api_resolve_legacy_auth_router_drift`: classify or retire `backend/app/routes/auth.py` as legacy-only so it no longer acts like live auth truth.

## informational
- None.
