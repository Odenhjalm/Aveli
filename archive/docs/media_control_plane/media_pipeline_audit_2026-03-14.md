

Current STATE 2026-03-14

**1. Executive Summary**
- Overall health is fragile, not enterprise-ready. Confirmed fact: runtime is not one media pipeline but at least three live contracts: `app.media_objects`, `app.media_assets`, and `app.lesson_media`. Evidence: [media_contract_implementation.md](/home/rodenhjalm/Aveli/media_contract_implementation.md#L220), [api_media.py](/home/rodenhjalm/Aveli/backend/app/routes/api_media.py#L307), live counts `media_objects=1081`, `media_assets=167`, `lesson_media=841`. Confidence: 98%.
- Most likely primary root cause is legacy/current contract collision plus storage/data drift. Secondary causes are bucket/path drift, audio-derived-path rigidity, stale lesson-media embeds, frontend absolute-URL gating, and migration/history drift. Confidence: 95%.
- Main user-visible symptoms are explained by that mix: missing or unsupported placeholders, vanished play buttons, silent image disappearance, and media that still has DB rows but no effective storage object or no renderable signed URL. Enterprise readiness score: `3/10`. Confidence: 95%.

**2. System Inventory**
- Backend current pipeline: [api_media.py](/home/rodenhjalm/Aveli/backend/app/routes/api_media.py#L307), [lesson_playback_service.py](/home/rodenhjalm/Aveli/backend/app/services/lesson_playback_service.py#L78), [media_assets.py](/home/rodenhjalm/Aveli/backend/app/repositories/media_assets.py#L151), [media_transcode_worker.py](/home/rodenhjalm/Aveli/backend/app/services/media_transcode_worker.py#L214), [media_resolver.py](/home/rodenhjalm/Aveli/backend/app/services/media_resolver.py#L104). Role: upload, asset state, signing, playback, transcoding. Status: active/current.
- Backend mixed or legacy: [upload.py](/home/rodenhjalm/Aveli/backend/app/routes/upload.py#L363), [media.py](/home/rodenhjalm/Aveli/backend/app/routes/media.py#L563), [studio.py](/home/rodenhjalm/Aveli/backend/app/routes/studio.py#L250), [models.py](/home/rodenhjalm/Aveli/backend/app/models.py#L1219), [courses_service.py](/home/rodenhjalm/Aveli/backend/app/services/courses_service.py#L445). Role: legacy upload, local/public URL fallback, object resolution, lesson media listing. Status: active but partially migrated.
- Frontend current student/runtime path: [lesson_media_playback_resolver.dart](/home/rodenhjalm/Aveli/frontend/lib/shared/utils/lesson_media_playback_resolver.dart#L5), [lesson_content_pipeline.dart](/home/rodenhjalm/Aveli/frontend/lib/shared/utils/lesson_content_pipeline.dart#L847), [lesson_page.dart](/home/rodenhjalm/Aveli/frontend/lib/features/courses/presentation/lesson_page.dart#L569), [AveliLessonMediaPlayer.dart](/home/rodenhjalm/Aveli/frontend/lib/shared/media/AveliLessonMediaPlayer.dart#L32), [AveliLessonImage.dart](/home/rodenhjalm/Aveli/frontend/lib/shared/media/AveliLessonImage.dart#L18). Status: active/current.
- Frontend studio and legacy bridges: [course_editor_page.dart](/home/rodenhjalm/Aveli/frontend/lib/features/studio/presentation/course_editor_page.dart#L1418), [media_pipeline_repository.dart](/home/rodenhjalm/Aveli/frontend/lib/features/media/data/media_pipeline_repository.dart#L285), [media_repository.dart](/home/rodenhjalm/Aveli/frontend/lib/features/media/data/media_repository.dart#L57). Status: mixed; image embedding still URL-based, not purely canonical-id based. `home_player_course_links` appears dead or unadopted in live data.

**3. Database Schema Review**
- `app.media_assets` is the processing/backing-asset table. Key columns: `id`, `owner_id`, `course_id`, `lesson_id`, `media_type`, `purpose`, `original_object_path`, `storage_bucket`, `streaming_object_path`, `streaming_storage_bucket`, `duration_seconds`, `codec`, `state`, `error_message`, retry/lock fields, timestamps. Source-of-truth implication: canonical for lesson audio and course covers, only partially canonical for other lesson media. No live RLS. Confidence: 98%.
- `app.lesson_media` is the authored attachment/embed surface. Key columns: `id`, `lesson_id`, `kind`, `media_id`, `media_asset_id`, `storage_bucket`, `storage_path`, `duration_seconds`, `position`, timestamps. It can point to `media_objects`, `media_assets`, or a direct storage tuple. That nullable multi-reference design is the core ambiguity. Confidence: 99%.
- `app.media_objects` remains runtime-relevant, not dead. Key columns: `id`, `owner_id`, `storage_bucket`, `storage_path`, `content_type`, `byte_size`, `checksum`, `original_name`, timestamps. `app.courses.cover_media_id -> media_assets.id`; `home_player_uploads` mixes `media_id` and `media_asset_id`; `home_player_course_links` has 14 rows and all `lesson_media_id` values are null. `storage.objects` is the real existence source of truth. `media_resolution_failures` is active telemetry; `lesson_media_issues` is empty.

**4. Storage Contract Review**
- Live buckets: private `audio_private`, `welcome-cards`, `brand`, `course-media`, `lesson-media`; public `public-media`. Path families in use: `media/source`, `media/derived`, `courses/`, `lessons/`, `home-player/`. Evidence: live bucket metadata and [media_paths.py](/home/rodenhjalm/Aveli/backend/app/utils/media_paths.py#L6). Confidence: 99%.
- The contract is inconsistent across DB, backend, and frontend. Docs describe `lesson-media` as legacy, but code still defaults legacy rows there; some rows store originals, some derived paths, some public URLs. Audio runtime expects derived/private objects while many records still resolve to object-backed or original-style paths. Confidence: 95%.
- Confirmed missing-object drift is large: `media_objects` includes 188 missing `course-media` audio objects; 178 object-backed audio `lesson_media` rows have no effective object; several `ready` `media_assets` rows are missing both original and streaming objects. Confidence: 99%.

**5. Upload Pipeline Review**
- Lesson audio current flow: frontend -> `POST /api/media/upload-url` -> direct upload to private storage -> `POST /api/media/upload-url/complete` -> `media_assets` + `lesson_media` -> worker -> ready derived audio -> embed/playback later uses `lesson_media.id`, not `media_assets.id`. Evidence: [api_media.py](/home/rodenhjalm/Aveli/backend/app/routes/api_media.py#L307). Confidence: 98%.
- Lesson image/video/PDF current flow is mostly passthrough: `POST /studio/lessons/{lesson_id}/media/presign` + `/complete` or `POST /api/upload/lesson-image`; backend persists `lesson_media` plus a ready passthrough `media_assets` row via [upload.py](/home/rodenhjalm/Aveli/backend/app/routes/upload.py#L363). Images are often embedded as absolute public URLs, not only canonical ids. Confidence: 95%.
- Course cover is the cleanest flow: `POST /api/media/cover-upload-url` -> upload -> worker -> public derived cover -> `courses.cover_media_id`. Legacy flows remain active through `/api/upload/course-media`, `/studio/lessons/{lesson_id}/media`, `/api/files/*`, and `media_objects`. That mixed ingestion is the biggest drift source. Confidence: 95%.

**6. Processing / Transcoding Review**
- Actual processing scope is narrow: only audio and course-cover images are transcoded/derived. Lesson videos, PDFs, and most lesson images are passthrough, not enterprise-style processing. Evidence: [media_transcode_worker.py](/home/rodenhjalm/Aveli/backend/app/services/media_transcode_worker.py#L214). Confidence: 99%.
- Audio state flow is `pending_upload -> uploaded -> processing -> ready/failed`; the worker only locks rows whose source object exists, so uploaded rows with missing source objects remain stuck and unplayable. Confidence: 98%.
- Fallback does not reliably work. For audio, if a `media_asset_id` exists and pipeline resolution fails, playback re-raises instead of falling back to legacy object playback; non-audio is more permissive. That is a confirmed reason previously working audio can become unavailable after partial migration. Confidence: 97%.

**7. Signing / Playback / Auth Review**
- `POST /api/media/playback-url`: auth required, input `{media_id}` where `media_id` is really `media_assets.id`, returns signed/absolute playback URL or 404/409/415, called by [media_pipeline_repository.dart](/home/rodenhjalm/Aveli/frontend/lib/features/media/data/media_pipeline_repository.dart#L285). Current, asset-level.
- `POST /api/media/lesson-playback`: auth required, input `{lesson_media_id}`, returns lesson playback URL or 404/409/415/503, called by [lesson_media_playback_resolver.dart](/home/rodenhjalm/Aveli/frontend/lib/shared/utils/lesson_media_playback_resolver.dart#L69). Current, student-facing.
- `POST /media/sign`: legacy signer, input `{media_id}` but semantically `lesson_media.id`, called by [media_repository.dart](/home/rodenhjalm/Aveli/frontend/lib/features/media/data/media_repository.dart#L57). `GET /media/stream/{token}` serves the tokenized stream. `GET /studio/media/{media_id}` is a legacy attachment route. Frontend/backend alignment is only partial because one UI path speaks asset ids, another lesson-media ids, and the renderer discards relative protected URLs entirely. Confidence: 95%.

**8. Content Embedding and Markdown Contract Review**
- Actual embed contract today is mixed. Custom tokens `!audio(uuid)`, `!video(uuid)`, `!image(uuid)` and `/studio/media/<uuid>` overwhelmingly store `lesson_media.id`; live resolution found `209/211` audio tokens, `61/62` video tokens, and `47/64` image tokens resolving to `lesson_media.id`, not `media_assets.id` or `media_objects.id`. Confidence: 99%.
- Images are the major outlier: 339 lessons still store absolute Supabase public URLs in markdown, and editor image insertion uses URL embeds rather than a canonical id-backed token. That means lesson attachments and markdown embeds are separate systems and do drift. Confidence: 98%.
- Stale references are live: unresolved embed ids exist for audio (2), image (17), and video (1). There are also 522 `lesson_media` rows not referenced in markdown, and 366 lessons with attachments that are not embedded. Confidence: 99%.

**9. Renderer Decision Logic Review**
- Audio/video controls render only when the frontend has an absolute `http/https` playback URL; otherwise [AveliLessonMediaPlayer.dart](/home/rodenhjalm/Aveli/frontend/lib/shared/media/AveliLessonMediaPlayer.dart#L32) shows “Media saknas eller stöds inte längre,” and runtime player errors collapse to the same state. Confidence: 99%.
- Images are more brittle: [AveliLessonImage.dart](/home/rodenhjalm/Aveli/frontend/lib/shared/media/AveliLessonImage.dart#L18) returns `SizedBox.shrink()` for non-absolute URLs and again on load error, so images can vanish with no visible error. Confidence: 99%.
- Renderer logic is brittle around `content_type`, kind, asset state, and URL nullability. Confirmed dirty rows include `lesson_media.kind='audio'` backed by `image/png` and `video/mp4`, which can route media into unsupported or misleading UI states. Confidence: 97%.

**10. Local vs Deployed Drift Review**
- Confirmed production backend route surface matches repo expectations; `https://aveli.fly.dev/openapi.json` exposes the media endpoints present in repo. Confidence: 95%.
- Confirmed drift exists in docs and migration history: [Supabase_MCP.md](/home/rodenhjalm/Aveli/Supabase_MCP.md#L1) references an old project ref while [.vscode/mcp.json](/home/rodenhjalm/Aveli/.vscode/mcp.json#L1) and [frontend/.env.web](/home/rodenhjalm/Aveli/frontend/.env.web#L5) use `aiftpfyrqjhstcnblyhb`; live DB lacks some repo migration files/views, yet also contains schema state not cleanly reflected in live migration history. Confidence: 98%.
- Frontend deploy parity is unverified. `app.aveli.app/version.json` shows a build from 2026-03-14, local repo HEAD is newer, and the workspace has an uncommitted change in `lesson_media_playback_resolver.dart`. Bundle drift is plausible, but the dominant failures are already explained by live data and contract drift. Confidence: 80%.

**11. Data Integrity Review**
- Confirmed broken references: 178 object-backed audio `lesson_media` rows have no effective object; uploaded asset-backed audio adds 14 more broken rows; 12 object-backed images and 11 of 12 PDF/document references are effectively broken; several `ready` assets are missing their objects. Confidence: 99%.
- Confirmed stale content: unresolved markdown ids remain, and some lessons mix legacy/public-URL images with id-backed audio/video. Confidence: 99%.
- Confirmed cross-table drift: `home_player_course_links` has 14 rows and all 14 have `lesson_media_id IS NULL`; `media_resolution_failures` holds 378 live failures, mostly `missing_object/student_render` (251) and `key_format_drift/student_render` (113). Confidence: 99%.

**12. End-to-End Path Matrix**
- Audio embedded in lesson body: editor upload -> `media_assets` + `lesson_media` -> tokenized `lesson_media.id` in markdown -> `POST /api/media/lesson-playback` -> audio player. Breakpoints: stale token, missing source/derived object, asset exists but audio fallback disabled, non-absolute URL rejected. Confidence: 98%.
- Audio attached to lesson: same backend contract, but the attachment list still includes many object-backed legacy rows; 178 such audio rows are already broken at storage existence. Confidence: 99%.
- Video embedded/attached: legacy object-backed flow still dominates and mostly survives; 66 object-backed rows have effective objects and 1 asset-backed row is fine. Breakage risk is lower and mostly tied to stale ids or URL resolution. Confidence: 90%.
- Image embedded/attached: upload often becomes public URL markup instead of canonical id; render success depends on absolute public URL and actual object existence. Breakpoints: 17 stale image tokens, 12 missing effective objects, silent shrink on invalid URL/load error. PDFs/documents are passthrough and fragile: 11 of 12 referenced rows lack an effective object. Course covers are the healthiest path end-to-end. Confidence: 97%.

**13. Failure Catalogue**
- `Legacy object loss`: symptom missing/unsupported media; cause `lesson_media/media_objects` rows outliving storage objects; subsystem data/storage; systemic; type data drift. Confidence: 99%.
- `Ready-but-missing asset`: symptom previously uploaded pipeline media fails; cause `media_assets.state='ready'` with missing original/streaming objects; subsystem processing/storage; systemic; mixed data/state issue. Confidence: 99%.
- `Audio no-fallback trap`: symptom play button disappears for migrated audio; cause asset-backed audio errors re-raised instead of falling back; subsystem playback contract; systemic; code+data issue. Confidence: 97%.
- `Relative URL suppression`: symptom controls disappear although backend can sign/serve; cause frontend rejects non-absolute protected URLs; subsystem renderer/auth; systemic; code-contract issue. Confidence: 95%.
- `URL image drift`: symptom editor/live mismatch or vanished images; cause image embeds stored as raw public URLs while canonical contract expects `lesson_media.id`; subsystem content model; systemic; mixed issue. Confidence: 98%.
- `Stale embed ids`: symptom isolated missing media blocks in lesson body; cause unresolved `lesson_media.id` tokens; subsystem content/data; recurring. Confidence: 99%.
- `Kind/content-type mismatch`: symptom unsupported or wrong renderer; cause dirty legacy rows; subsystem renderer/data; isolated but dangerous. Confidence: 95%.
- `Schema/history drift`: symptom hard-to-explain prod behavior and repair uncertainty; cause live schema not matching repo migration history/docs exactly; subsystem ops/migrations; systemic. Confidence: 90%.

**14. Invariants and Contract Violations**
- `Every embedded media token resolves to one canonical entity`: violated by unresolved tokens and URL-based image embeds. Severity: critical.
- `Every ready asset has an existing storage object`: violated by ready `media_assets` missing original/streaming files. Severity: critical.
- `Lesson attachments and embedded content stay aligned`: violated by 522 unattached-to-content rows and widespread URL/image drift. Severity: high.
- `Frontend and backend agree on identifier type and URL form`: violated by asset-id vs lesson-media-id split and frontend absolute-URL requirement. Severity: critical.
- `Audio playback has a deterministic signed fallback`: violated by derived-audio-only requirement and no fallback for failed pipeline audio. Severity: critical.
- `Bucket/path semantics are explicit and stable`: violated by `course-media`, `public-media`, and legacy `lesson-media` coexistence plus key-format drift telemetry. Severity: high.

**15. Enterprise Readiness Assessment**
- Current media pipeline is not enterprise-ready. Reliability `3/10`, consistency `2/10`, observability `6/10`, debuggability `4/10`, maintainability `3/10`, migration safety `2/10`, runtime determinism `3/10`, UX stability `3/10`.
- Biggest blockers are absence of one canonical media contract, storage-existence drift, partial migration between `media_objects` and `media_assets`, and renderer/auth behavior that converts recoverable backend failures into silent or generic UI loss. Confidence: 95%.

**16. Final Conclusion**
- Single most likely primary root cause: a mixed-contract media system where `lesson_media`, `media_assets`, and `media_objects` all remain active while storage reality has drifted away from DB/runtime assumptions. Confidence: 99%.
- Major secondary causes are audio-derived-path rigidity, public/private bucket/path inconsistency, URL-based image embeds, stale `lesson_media` ids, and schema/migration/documentation drift. This is primarily a mixed code-contract plus data-drift issue, with secondary deploy/history drift. Confidence: 95%.
- Minimum safe next-phase remediation scope must treat authored identity, backing asset identity, storage existence verification, audio signing/playback, image/PDF embedding, and legacy migration as one coordinated program. The future canonical design should make `lesson_media.id` the only authored/embed/playback reference and `media_assets` the only canonical byte/processing record, with verified storage-object invariants. Confidence: 90%.

**17. Appendix**
- Key evidence files: [api_media.py](/home/rodenhjalm/Aveli/backend/app/routes/api_media.py#L307), [upload.py](/home/rodenhjalm/Aveli/backend/app/routes/upload.py#L363), [studio.py](/home/rodenhjalm/Aveli/backend/app/routes/studio.py#L250), [lesson_playback_service.py](/home/rodenhjalm/Aveli/backend/app/services/lesson_playback_service.py#L78), [media_resolver.py](/home/rodenhjalm/Aveli/backend/app/services/media_resolver.py#L104), [media_transcode_worker.py](/home/rodenhjalm/Aveli/backend/app/services/media_transcode_worker.py#L214), [courses_service.py](/home/rodenhjalm/Aveli/backend/app/services/courses_service.py#L445), [models.py](/home/rodenhjalm/Aveli/backend/app/models.py#L1219), [lesson_media_playback_resolver.dart](/home/rodenhjalm/Aveli/frontend/lib/shared/utils/lesson_media_playback_resolver.dart#L5), [lesson_content_pipeline.dart](/home/rodenhjalm/Aveli/frontend/lib/shared/utils/lesson_content_pipeline.dart#L847), [lesson_page.dart](/home/rodenhjalm/Aveli/frontend/lib/features/courses/presentation/lesson_page.dart#L569), [AveliLessonMediaPlayer.dart](/home/rodenhjalm/Aveli/frontend/lib/shared/media/AveliLessonMediaPlayer.dart#L32), [AveliLessonImage.dart](/home/rodenhjalm/Aveli/frontend/lib/shared/media/AveliLessonImage.dart#L18), [course_editor_page.dart](/home/rodenhjalm/Aveli/frontend/lib/features/studio/presentation/course_editor_page.dart#L1418), [MEDIA_CONTRACT_v1.md](/home/rodenhjalm/Aveli/docs/MEDIA_CONTRACT_v1.md#L39), [media_contract_implementation.md](/home/rodenhjalm/Aveli/media_contract_implementation.md#L220).
- Key routes: `POST /api/media/upload-url`, `POST /api/media/upload-url/complete`, `POST /api/media/playback-url`, `POST /api/media/lesson-playback`, `POST /api/media/cover-upload-url`, `POST /media/sign`, `GET /media/stream/{token}`, `POST /studio/lessons/{lesson_id}/media/presign`, `POST /studio/lessons/{lesson_id}/media/complete`, `GET /studio/media/{media_id}`, legacy `POST /api/upload/course-media`, `POST /api/upload/lesson-image`. Key tables: `app.media_assets`, `app.lesson_media`, `app.media_objects`, `app.lessons`, `app.courses`, `storage.objects`, `app.home_player_uploads`, `app.home_player_course_links`, `app.media_resolution_failures`, `app.lesson_media_issues`. Buckets/paths: `course-media`, `public-media`, `lesson-media`, `audio_private`, with `media/source`, `media/derived`, `courses`, `lessons`, `home-player`.
- Representative read-only queries used through the repo’s Supabase MCP shim because direct in-session MCP bindings were permission-blocked: asset/storage joins for missing originals and derived objects, `lesson_media` effective-object rollups by `kind` and reference mode, regex extraction of `!audio(...)`/`!video(...)`/`!image(...)` ids from `lessons.content_markdown`, rollups of `app.media_resolution_failures`, and bucket/path family counts from `storage.objects`. Representative failing ids: `329faa90-21c6-477f-aff2-587a6d527e4e`, `33d8004e-525c-4dbe-a4b4-b99e4a1d9678`, `f099959e-9f62-412f-a2f1-9601afe3777b`, `0390646a-4e8f-4966-9751-e464b16ca41c`, `57157f11-dab5-4c13-8256-8f93f0aa59aa`. Representative embeds: `!audio(79306314-bb2f-426f-a723-e26c7e5fb4e6)`, `!video(24cbd3eb-e343-4e90-bc10-df00a45a4602)`, `!image(dc57c46a-8539-406a-99b0-4b7ff2500b2e)`, and absolute `https://aiftpfyrqjhstcnblyhb.supabase.co/storage/v1/object/public/public-media/...` image URLs.
- Evidence vs inference: confirmed facts come from repo files, live read-only Supabase inspection, and safe GET/HEAD checks; strong inferences concern deploy parity and which exact frontend bundle is live; uncertain areas are exact frontend commit parity and any auth-only runtime branch that could not be replayed safely without mutating state.