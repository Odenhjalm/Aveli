# Media Architecture

Den här filen var en tidig skiss och var på väg att bli en “legacy-fälla”.
Aktuell käll-of-truth finns här:

- Policy (buckets, public vs private): `docs/storage_buckets.md`
- Robusthet + resolverregler: `docs/media_robustness.md`
- WAV ingest + transcode pipeline: `docs/media_pipeline.md`

Om du behöver en full arkitekturöversikt för onboarding: uppdatera `ARCHITECTURE.md`
istället för att återintroducera parallella dokument med egna antaganden.
