# Home Player

## USER STATES
- Authenticated teacher-capable session on the Home-player control panel.

## UI ELEMENTS
- Page title `Home-spelarens bibliotek`.
- Section heading `Media för Home-spelaren`.
- Buttons `Ladda upp` and `Uppdatera`.
- Helper text stating uploads here are direct for Home and separate from courses.
- Long media list where each observed row showed a type label `Ljud`, a track button, a `Ta bort` button, and a checked switch.
- Section `Länkat från kurser` with `Länka media`, `Uppdatera`, explanatory text, and the empty-state text `Inga länkar ännu.`
- Footer buttons included `Hem`, `Terms of Service`, `Privacy Policy`, and `Data Deletion`.

## ACTIONS
- The teacher dashboard button `Öppna spelarens kontrollpanel` opened this page.
- Individual media-row buttons, `Ta bort` buttons, and switches were visible but not used.
- `Tillbaka` was used once from this page.

## DISABLED / HIDDEN
- No linked-course media rows were visible.
- No disabled controls were observed.

## TRANSITIONS
- Teacher-home `Öppna spelarens kontrollpanel` navigated to this surface.
- `Tillbaka` returned to `#/home` in the observed flow.

## RULES
- The page text explicitly separated Home uploads from course-linked media.
