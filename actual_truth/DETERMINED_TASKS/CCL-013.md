# TASK ID: CCL-013

## TITLE
Studio editor hydration lifecycle safety

## TYPE
FRONTEND_ALIGNMENT

## ROLE
OWNER

## DOMAIN TAG
lesson-content

## SOURCE AUTHORITY
actual_truth/DETERMINED_TASKS/CCL_course_cover_lesson_content_authority_task_tree.md

## DEPENDS_ON
[CCL-012]

## STATUS
DONE

## MUTATION PLANE
frontend editor page/state lifecycle

## OBJECTIVE
Rewrite studio editor hydration lifecycle to load persisted lesson content from the dedicated content read endpoint only. The canonical concurrency model is backend `ETag` on content read plus `If-Match` on content write. The editor must store the latest successful content token per selected lesson, include that token on save, and treat stale-write failures as non-persisted conflict states that require rehydration before retry. Failed hydration must keep save disabled for that lesson until a successful content read or explicit user recovery path obtains a new token. Empty content is valid only when it comes from successful hydration of persisted empty content, or when the user intentionally clears content after successful hydration; it must never be persisted because structure data lacked content, a content read failed, a lesson switch raced, or the editor initialized with an empty fallback. The editor must guard stale requests during lesson/course switches, preserve dirty state intentionally, and align read/write surfaces so saving cannot overwrite existing content unintentionally.

## EXPECTED OUTCOME
Widget tests prove persisted content loads through the endpoint, the current `ETag` is used on save, stale-write failures do not persist, switching lessons is stable, failed content reads fail closed, intentional empty clears are distinguishable from fallback empty state, and save does not fire from empty structure hydration.

## CONSTRAINTS
Execute only after DEPENDS_ON is satisfied. Use only the declared MUTATION PLANE. Preserve source task meaning exactly. Do not create parallel authority paths, hidden fallbacks, or UI authority.

## VERIFICATION
Widget tests prove persisted content loads through the endpoint, the current `ETag` is used on save, stale-write failures do not persist, switching lessons is stable, failed content reads fail closed, intentional empty clears are distinguishable from fallback empty state, and save does not fire from empty structure hydration.

## STOP CONDITIONS
Apply the STOP CONDITIONS FOR FUTURE EXECUTION from the SOURCE AUTHORITY exactly. No task-specific override is defined here.

## EVIDENCE TO COLLECT
Current repo source evidence within the declared mutation plane; modified files if future execution mutates within the declared mutation plane; verification output required by this task; task artifact status update and execution notes.

## EXECUTION NOTES
STATUS set to IN_PROGRESS after verifying CCL-012 is DONE in the task ledger, CCL-011 is DONE as prerequisite backend authority evidence, and CCL_INDEX.md lists CCL-013 as the only current eligible task.

Execution summary:
- Rewrote studio editor hydration so selected lesson content is loaded only through `StudioRepository.readLessonContent`, which maps to `GET /studio/lessons/{lesson_id}/content`.
- Removed the previous structure-derived empty fallback in editor boot; lesson structure now supplies only `lesson_title` while persisted markdown comes from the dedicated content read result.
- Added editor state for the latest backend content `ETag`, the hydrated lesson id, and the content hydration error state.
- Made editor readiness require selected lesson id, matching request id, hydrated lesson id, no hydration error, and a non-empty backend `ETag`.
- Added explicit error boot state and retry surface for failed hydration and missing/invalid content tokens.
- Blocked editor mutations, title dirtying, preview mode, reset, and save until the selected lesson is fully hydrated with a valid content token.
- Reworked save to call `StudioRepository.updateLessonContent` with the stored `If-Match` token before structure update when markdown changed, so stale content failures do not perform the paired structure save first.
- Preserved intentional empty clears because an empty markdown write can only occur after successful hydration and user edit; empty content is no longer produced by structure fallback, failed hydration, or lesson switching.
- Added a narrow-layout editor surface gate so mobile/narrow rendering no longer bypasses the hydration/error shell.
- Kept backend code, contracts, SQL, and CCL-014 untouched.

Files inspected:
- actual_truth/DETERMINED_TASKS/CCL_INDEX.md
- actual_truth/DETERMINED_TASKS/CCL-011.md
- actual_truth/DETERMINED_TASKS/CCL-012.md
- actual_truth/DETERMINED_TASKS/CCL-013.md
- actual_truth/DETERMINED_TASKS/CCL_course_cover_lesson_content_authority_task_tree.md
- frontend/lib/features/studio/presentation/course_editor_page.dart
- frontend/lib/features/studio/data/studio_repository.dart
- frontend/lib/features/studio/data/studio_models.dart
- frontend/test/widgets/course_editor_screen_test.dart
- frontend/test/unit/studio_repository_lesson_content_read_test.dart

Files changed:
- frontend/lib/features/studio/presentation/course_editor_page.dart
- frontend/test/widgets/course_editor_lesson_content_lifecycle_test.dart
- actual_truth/DETERMINED_TASKS/CCL-013.md
- actual_truth/DETERMINED_TASKS/CCL_INDEX.md

Commands run:
- `rg` scans for `readLessonContent`, `updateLessonContent`, `upsertLesson`, `content_markdown`, `contentMarkdown`, `?? ''`, `If-Match`, ETag state, stale status handling, and narrow editor rendering.
- `dart format frontend/lib/features/studio/presentation/course_editor_page.dart`
- `dart format frontend/test/widgets/course_editor_lesson_content_lifecycle_test.dart`
- `flutter analyze lib/features/studio/presentation/course_editor_page.dart lib/features/studio/data/studio_repository.dart lib/features/studio/data/studio_models.dart`
- `flutter analyze lib/features/studio/presentation/course_editor_page.dart test/widgets/course_editor_lesson_content_lifecycle_test.dart`
- `flutter analyze test/widgets/course_editor_lesson_content_lifecycle_test.dart`
- `flutter analyze lib/features/studio/presentation/course_editor_page.dart`
- `flutter test test/unit/studio_repository_lesson_content_read_test.dart`
- `flutter test test/widgets/course_editor_screen_test.dart --plain-name "CourseEditorScreen renders provided course data"`
- `flutter test test/widgets/course_editor_lesson_content_lifecycle_test.dart`
- `git diff --check`

Evidence collected:
- Source scan shows mounted editor lifecycle uses `_studioRepo.readLessonContent(lessonId)` for hydration and `_studioRepo.updateLessonContent(... ifMatch: contentEtag)` for content writes.
- Source scan shows no positive `course_editor_page.dart` call to `_studioRepo.upsertLesson`.
- Source scan shows no `lesson?.contentMarkdown ?? ''` or `const storedMarkdown = ''` fallback remains in editor boot.
- Source scan found only an unrelated `media.originalName ?? ''` fallback in media filename labeling, not lesson content hydration.
- Editor readiness now requires non-empty `_lastSavedLessonContentEtag`, matching `_lessonContentHydratedLessonId`, matching request id, and no `_lessonContentLoadError`.
- Failed hydration sets `_LessonEditorBootPhase.error`, clears the content token through `_resetLessonEditorBootValues`, keeps dirty false, and renders Swedish retry copy.
- Stale/tokenless content write responses `412` and `428` move the editor into an explicit error state and require rehydration before retry.
- Save runs content write before structure update when markdown changed, so stale content writes do not first persist the paired structure update.
- Narrow layout now calls `_buildNarrowLessonEditorSurface`, which gates edit and preview rendering behind `_isSelectedLessonDocumentReady`.
- Focused widget test `frontend/test/widgets/course_editor_lesson_content_lifecycle_test.dart` proves hydration from content read, save with the read ETag, and failed content read fail-closed without content update.
- Existing broad widget test file `frontend/test/widgets/course_editor_screen_test.dart` was probed and failed to compile because of stale typed-model/test fixture drift unrelated to CCL-013 implementation; that broad alignment remains owned by downstream test-alignment work, not this editor lifecycle mutation.

Verification result:
- PASS: `flutter test test/widgets/course_editor_lesson_content_lifecycle_test.dart`
- PASS: `flutter test test/unit/studio_repository_lesson_content_read_test.dart`
- PASS: `flutter analyze test/widgets/course_editor_lesson_content_lifecycle_test.dart`
- PASS: `git diff --check`
- INFO ONLY: `flutter analyze lib/features/studio/presentation/course_editor_page.dart` reports the same two pre-existing `use_build_context_synchronously` infos at lines 4908 and 4918, outside the CCL-013 edits.
- BLOCKED OUT-OF-SCOPE TEST SUBSTRATE: `flutter test test/widgets/course_editor_screen_test.dart --plain-name "CourseEditorScreen renders provided course data"` fails during compilation because the broad legacy widget file still uses stale map-based model fixtures, old `upsertLesson` named arguments, old course summary constructors, and old preview cache/upload job fields. CCL-016 is the downstream frontend test-alignment owner for broad widget test realignment.

Next eligible task(s):
- CCL-014
