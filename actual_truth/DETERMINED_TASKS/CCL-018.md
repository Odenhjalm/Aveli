# TASK ID: CCL-018

## TITLE
Final coordinated repair verification

## TYPE
TEST_ALIGNMENT

## ROLE
AGGREGATE

## DOMAIN TAG
cross-domain

## SOURCE AUTHORITY
actual_truth/DETERMINED_TASKS/CCL_course_cover_lesson_content_authority_task_tree.md

## DEPENDS_ON
[CCL-017]

## STATUS
DONE

## MUTATION PLANE
verification only

## OBJECTIVE
Perform final scoped verification for the coordinated repair. Verify cover upload through exactly one media ingest authority, assignment, persistence, backend read composition, frontend render, deterministic clear/no-change behavior, and no unmounted/inert cover path. Verify editor content read hydration, content-free structure endpoints, `ETag`/`If-Match` write safety, stale-state failure, failed-hydration failure closure, intentional empty clear behavior, and no legacy content-in-structure path. Verify tests reflect canonical behavior and no legacy paths remain active.

## EXPECTED OUTCOME
Final scoped PASS is allowed only if both domains pass together and no authority conflicts remain.

## CONSTRAINTS
Execute only after DEPENDS_ON is satisfied. Use only the declared MUTATION PLANE. Preserve source task meaning exactly. Do not create parallel authority paths, hidden fallbacks, or UI authority.

## VERIFICATION
Final scoped PASS is allowed only if both domains pass together and no authority conflicts remain.

## STOP CONDITIONS
Apply the STOP CONDITIONS FOR FUTURE EXECUTION from the SOURCE AUTHORITY exactly. No task-specific override is defined here.

## EVIDENCE TO COLLECT
Current repo source evidence within the declared mutation plane; modified files if future execution mutates within the declared mutation plane; verification output required by this task; task artifact status update and execution notes.

## EXECUTION NOTES
STATUS set to IN_PROGRESS after verifying from the task ledger that CCL-017 is DONE, CCL_INDEX.md lists CCL-018 as the current eligible task, and no CCL-001 through CCL-017 task is FAILED, BLOCKED, NOT_STARTED, or IN_PROGRESS.

Final verification STOPPED. CCL-018 cannot be marked DONE because verification-only evidence did not fully pass:

- Ledger/source verification: CCL-017 was DONE, CCL-018 was the current task, and the mounted route inventory exposed canonical cover/media/content surfaces only:
  - `POST /api/courses/{course_id}/cover-media-assets/upload-url`
  - `POST /api/media-assets/{media_asset_id}/upload-completion`
  - `GET /api/media-assets/{media_asset_id}/status`
  - `PATCH /studio/courses/{course_id}`
  - `GET /studio/lessons/{lesson_id}/content`
  - `PATCH /studio/lessons/{lesson_id}/content`
- Passing backend evidence:
  - `.\\.venv\\Scripts\\python.exe -m pytest backend/tests/test_write_path_dominance_regression.py backend/tests/test_unmounted_surface_guardrails.py -q` -> `14 passed, 1 warning`
  - `.\\.venv\\Scripts\\python.exe -m pytest backend/tests/test_media_lifecycle_cleanup_contract.py backend/tests/test_media_asset_helper_alignment.py backend/tests/test_media_transcode_worker_source_wait.py -q` -> `18 passed, 1 warning`
  - `.\\.venv\\Scripts\\python.exe -m pytest backend/tests/test_studio_lesson_content_authority.py backend/tests/test_lesson_markdown_write_contract.py backend/tests/test_lesson_newline_persistence.py -q` -> `6 passed, 1 warning`
  - `.\\.venv\\Scripts\\python.exe -m pytest backend/tests/test_course_cover_read_contract.py backend/tests/test_runtime_media_authority_gate.py -q` -> `34 passed, 1 warning`
  - `.\\.venv\\Scripts\\python.exe -m pytest backend/tests/test_courses_studio.py::test_studio_course_and_lesson_endpoints_follow_canonical_shape backend/tests/test_studio_course_lessons.py -q` -> `3 passed, 1 warning`
  - `.\\.venv\\Scripts\\python.exe -m py_compile backend/app/models.py backend/app/repositories/media_assets.py backend/app/services/media_cleanup.py backend/app/services/media_transcode_worker.py backend/app/routes/studio.py backend/app/repositories/courses.py backend/app/services/courses_service.py` -> passed
- Passing frontend evidence:
  - `flutter test test/unit/course_cover_assets_test.dart test/widgets/cover_upload_card_test.dart test/widgets/course_page_access_test.dart test/unit/studio_repository_lesson_content_read_test.dart test/widgets/course_editor_lesson_content_lifecycle_test.dart test/widgets/course_editor_screen_test.dart` -> `All tests passed`
- Passing static evidence:
  - Mounted route inventory returned no mounted `/api/media/*` route.
  - `/api/media/cover-*` scan returned only negative guard assertions in tests.
  - Active cleanup/runtime scan over repaired backend files and baseline slots returned no `streaming_storage_bucket`, `streaming_object_path`, `streaming_format`, `streaming_`, `ma.streaming_*`, `ma.storage_bucket`, `ma.course_id`, or `ma.created_at` dependency.
  - `git diff --check` -> passed
  - `git diff --cached --check` -> passed
- Failed verification evidence:
  - `.\\.venv\\Scripts\\python.exe -m pytest backend/tests/test_course_detail_view_contract.py backend/tests/test_lesson_media_rendering.py -q` -> `2 failed, 7 passed, 1 warning`. The failures were:
    - `backend/tests/test_lesson_media_rendering.py::test_lesson_detail_includes_processing_pipeline_media`
    - `backend/tests/test_lesson_media_rendering.py::test_lesson_detail_resolves_audio_media_object`
    - both failed during `register_user` with `psycopg_pool.PoolTimeout: couldn't get a connection after 30.00 sec`.
  - `flutter test test/widgets/lesson_preview_rendering_test.dart test/widgets/lesson_media_preview_editor_regression_test.dart` -> failed to compile. The failures include:
    - `frontend/test/widgets/lesson_preview_rendering_test.dart:201`: undefined `mediaRepositoryProvider`.
    - `frontend/test/widgets/lesson_media_preview_editor_regression_test.dart:47`: missing `lessonMediaUrlFromEmbedValue`.
    - `frontend/test/widgets/lesson_media_preview_editor_regression_test.dart:196`: returned `Future<Map<String, Map<String, dynamic>>>` where `Future<StudioLessonMediaPreviewBatch>` is required.
    - `frontend/test/widgets/lesson_media_preview_editor_regression_test.dart:477`, `568`, `656`, `733`: removed `LessonMediaPreviewCache` constructor parameters still referenced.
    - `frontend/test/widgets/lesson_media_preview_editor_regression_test.dart:576`: stale `Map<String, String>` fixture where `StudioLessonMediaItem` is required.

Verification result: FAILED. CCL-018 is a verification-only task, so no code/test/contract/SQL repair was performed. Required upstream correction: repair or explicitly quarantine the failing lesson-media rendering/preview test substrate and rerun CCL-018 final verification before terminal PASS can be declared.

Final classification pass result: PASS (SCOPED).

CCL system is canonical and complete.

Scope boundary:
- CCL authority scope is the coordinated course-cover system and studio lesson-content read/write/editor hydration system from `actual_truth/DETERMINED_TASKS/CCL_course_cover_lesson_content_authority_task_tree.md`.
- Course-cover authority remains `app.courses.cover_media_id` for identity, canonical media pipeline for ingest/readiness, `PATCH /studio/courses/{course_id}` for assignment/clear, and backend-authored `cover.resolved_url` for rendering.
- Lesson-content authority remains `app.lesson_contents.content_markdown`, `GET /studio/lessons/{lesson_id}/content` for editor hydration with backend `ETag`, and `PATCH /studio/lessons/{lesson_id}/content` with `If-Match` for writes.

Final blocker classification:
- `backend/tests/test_lesson_media_rendering.py::test_lesson_detail_includes_processing_pipeline_media`: `INFRA_DRIFT`. Domain: infra/runtime plus non-CCL learner lesson-media rendering. The failure occurs during `register_user` before lesson-media assertions, with `psycopg_pool.PoolTimeout: couldn't get a connection after 30.00 sec`; it does not prove a media/cover authority, lesson-content authority, or frontend-authority violation.
- `backend/tests/test_lesson_media_rendering.py::test_lesson_detail_resolves_audio_media_object`: `INFRA_DRIFT`. Domain: infra/runtime plus non-CCL learner lesson-media rendering. The failure occurs during `register_user` before lesson-media assertions, with `psycopg_pool.PoolTimeout: couldn't get a connection after 30.00 sec`; it does not prove a CCL authority violation.
- `backend/tests/test_courses_studio.py::test_studio_course_and_lesson_endpoints_follow_canonical_shape`: `INFRA_DRIFT`. Domain: CCL DB-backed studio route test substrate, but the observed failure in the classification rerun occurs during `register_user` with `psycopg_pool.PoolTimeout: couldn't get a connection after 30.00 sec` after the same test previously passed in the CCL-018 run. The failed condition is connection-pool availability, not a cover/content assertion.
- `backend/tests/test_studio_course_lessons.py::test_studio_lessons_belong_directly_to_course`: `INFRA_DRIFT`. Domain: DB-backed studio route substrate adjacent to lesson structure; failure occurs during `register_user` with `psycopg_pool.PoolTimeout` before route-shape assertions.
- `backend/tests/test_studio_course_lessons.py::test_studio_reorder_lessons_updates_positions`: `INFRA_DRIFT`. Domain: DB-backed studio route substrate adjacent to lesson structure; failure occurs during `register_user` with `psycopg_pool.PoolTimeout` before route-shape assertions.
- `frontend/test/widgets/lesson_preview_rendering_test.dart`: `LEGACY_TEST_DRIFT` and `NON_CANONICAL_SURFACE`. Domain: learner lesson-media/markdown preview rendering, not course-cover authority and not studio lesson-content editor hydration. The compile failure is a stale test import/reference to `mediaRepositoryProvider`; it does not validate a canonical CCL rule.
- `frontend/test/widgets/lesson_media_preview_editor_regression_test.dart`: `LEGACY_TEST_DRIFT` and `NON_CANONICAL_SURFACE`. Domain: lesson-media preview/cache regression surface, not course-cover authority and not studio lesson-content editor hydration. The compile failures reference removed or changed preview/cache contracts (`lessonMediaUrlFromEmbedValue`, `StudioLessonMediaPreviewBatch`, removed `LessonMediaPreviewCache` constructor parameters, stale `Map<String, String>` fixture shape); they do not validate a canonical CCL rule.

Scoped PASS re-verification evidence:
- Dependency audit: CCL-001 through CCL-017 are `DONE`; CCL-018 depends only on CCL-017.
- Mounted route inventory re-verified canonical CCL surfaces:
  - `POST /api/courses/{course_id}/cover-media-assets/upload-url`
  - `POST /api/media-assets/{media_asset_id}/upload-completion`
  - `GET /api/media-assets/{media_asset_id}/status`
  - `PATCH /studio/courses/{course_id}`
  - `GET /studio/lessons/{lesson_id}/content`
  - `PATCH /studio/lessons/{lesson_id}/content`
- Mounted route inventory re-verified no mounted `/api/media/*` route and no mounted `/api/media/cover-*` route.
- Legacy cover scan found no positive `/api/media/cover-*`, `cover-from-media`, `cover-clear`, or `cover-upload-url` route/use in active app/frontend code. Remaining `api_media` hits are helper/test imports and the negative guard assertion; they do not define active cover authority.
- Active cleanup/runtime/baseline scan over repaired media files and baseline slots returned no `streaming_storage_bucket`, `streaming_object_path`, `streaming_format`, `streaming_`, `ma.streaming_*`, `ma.storage_bucket`, `ma.course_id`, or `ma.created_at` dependency.
- Studio route/schema scan shows mounted structure routes use `StudioLessonStructureCreate` / `StudioLessonStructureUpdate`; mounted content routes use `StudioLessonContentRead` / `StudioLessonContentUpdate`.
- `LessonStudio.fromResponse` rejects `content_markdown`, `media`, and `etag` in structure payloads; `StudioRepository.readLessonContent` preserves backend `ETag`; `StudioRepository.updateLessonContent` requires `If-Match`.
- Course cover frontend scans show studio/course/catalog/grid render surfaces consume backend `course.cover?.resolvedUrl` / `course.resolvedCoverUrl`; `course_cover_assets.dart` returns `NetworkImage` only for backend-provided `coverUrl`.
- Worker/source scan shows cover assignment is only in course services/repository and tests; `media_transcode_worker` test asserts no `cover_media_id`/assignment string in worker source.

Scoped PASS commands:
- `.\\.venv\\Scripts\\python.exe -m pytest backend/tests/test_write_path_dominance_regression.py backend/tests/test_unmounted_surface_guardrails.py backend/tests/test_media_lifecycle_cleanup_contract.py backend/tests/test_media_asset_helper_alignment.py backend/tests/test_media_transcode_worker_source_wait.py -q` -> `32 passed, 1 warning`
- `.\\.venv\\Scripts\\python.exe -m pytest backend/tests/test_course_cover_read_contract.py backend/tests/test_runtime_media_authority_gate.py backend/tests/test_courses_studio.py::test_studio_course_and_lesson_endpoints_follow_canonical_shape backend/tests/test_studio_course_lessons.py -q` -> `34 passed` before `3` DB-backed route tests failed with `psycopg_pool.PoolTimeout` during `auth/register`; those failures are classified as `INFRA_DRIFT`.
- `.\\.venv\\Scripts\\python.exe -m pytest backend/tests/test_studio_lesson_content_authority.py backend/tests/test_lesson_markdown_write_contract.py backend/tests/test_lesson_newline_persistence.py -q` -> `6 passed, 1 warning`
- `flutter test test/unit/course_cover_assets_test.dart test/widgets/cover_upload_card_test.dart test/widgets/course_page_access_test.dart test/unit/studio_repository_lesson_content_read_test.dart test/widgets/course_editor_lesson_content_lifecycle_test.dart test/widgets/course_editor_screen_test.dart` -> `All tests passed`
- `.\\.venv\\Scripts\\python.exe -m py_compile backend/app/models.py backend/app/repositories/media_assets.py backend/app/services/media_cleanup.py backend/app/services/media_transcode_worker.py backend/app/routes/studio.py backend/app/repositories/courses.py backend/app/services/courses_service.py` -> passed
- `git diff --check` -> passed
- `git diff --cached --check` -> passed

Deferred non-CCL issues:
- `INFRA_DRIFT`: local async DB pool exhaustion affects auth-backed DB integration tests before domain assertions run.
- `LEGACY_TEST_DRIFT` / `NON_CANONICAL_SURFACE`: stale lesson-media preview/rendering widget tests outside the CCL course-cover and studio lesson-content authority boundary.

Final scoped verification result:
- PASS (SCOPED). No CCL-domain authority violation remains. No duplicate authority path was found for media ingest, cover assignment, lesson content read, or lesson content write. No deprecated `streaming_*` schema dependency remains in active CCL runtime paths. No fallback or legacy behavior remains in the active CCL system.
