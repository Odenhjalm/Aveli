# TASK ID: CCL-015

## TITLE
Backend lesson-content read test alignment gate

## TYPE
TEST_ALIGNMENT

## ROLE
GATE

## DOMAIN TAG
lesson-content

## SOURCE AUTHORITY
actual_truth/DETERMINED_TASKS/CCL_course_cover_lesson_content_authority_task_tree.md

## DEPENDS_ON
[CCL-014]

## STATUS
DONE

## MUTATION PLANE
backend tests only

## OBJECTIVE
Align backend lesson-content read tests. Tests must prove the dedicated editor content read returns persisted `content_markdown`, the locked body `{ lesson_id, content_markdown, media }`, and the backend-issued `ETag`; structure list/create/update responses remain content-free; content write/read round trips preserve backend-normalized markdown; writes require matching `If-Match`; stale writes fail without persistence; unauthorized reads are rejected; and no structure endpoint can become content authority.

## EXPECTED OUTCOME
Scoped backend lesson-content tests pass and fail closed on content leakage into structure endpoints.

## CONSTRAINTS
Execute only after DEPENDS_ON is satisfied. Use only the declared MUTATION PLANE. Preserve source task meaning exactly. Do not create parallel authority paths, hidden fallbacks, or UI authority.

## VERIFICATION
Scoped backend lesson-content tests pass and fail closed on content leakage into structure endpoints.

## STOP CONDITIONS
Apply the STOP CONDITIONS FOR FUTURE EXECUTION from the SOURCE AUTHORITY exactly. No task-specific override is defined here.

## EVIDENCE TO COLLECT
Current repo source evidence within the declared mutation plane; modified files if future execution mutates within the declared mutation plane; verification output required by this task; task artifact status update and execution notes.

## EXECUTION NOTES
STATUS set to IN_PROGRESS after verifying CCL-014 is DONE in the task ledger, CCL-011 through CCL-014 are DONE as prerequisite content authority evidence, and CCL_INDEX.md lists CCL-015 as the only current eligible task.

STATUS set to DONE after backend test alignment completed within the declared backend-tests-only mutation plane. Updated backend lesson-content tests to fetch the canonical content-read `ETag` before writes and send `If-Match`; added focused canonical content-authority coverage proving `{ lesson_id, content_markdown, media }` read shape, structure responses without `content_markdown`/`media`/`etag`, missing-token rejection `428`, stale-token rejection `412`, write/read roundtrip persistence, unauthorized read rejection, and no accepted mixed structure/content update. Updated the logs MCP backend test helper only to remove a positive legacy `/studio/lessons` mixed content creation path and to use canonical course/lesson/media test substrate.

Files changed for CCL-015: `backend/tests/test_courses_studio.py`, `backend/tests/test_studio_course_lessons.py`, `backend/tests/test_lesson_markdown_write_contract.py`, `backend/tests/test_lesson_newline_persistence.py`, `backend/tests/test_studio_lesson_content_authority.py`, `backend/tests/test_logs_mcp.py`, `actual_truth/DETERMINED_TASKS/CCL-015.md`, and `actual_truth/DETERMINED_TASKS/CCL_INDEX.md`.

Commands run: `.\\.venv\\Scripts\\python.exe -m py_compile backend/tests/test_courses_studio.py backend/tests/test_studio_course_lessons.py backend/tests/test_lesson_markdown_write_contract.py backend/tests/test_lesson_newline_persistence.py backend/tests/test_logs_mcp.py backend/tests/test_studio_lesson_content_authority.py`; `.\\.venv\\Scripts\\python.exe -m pytest backend/tests/test_studio_lesson_content_authority.py backend/tests/test_studio_course_lessons.py backend/tests/test_courses_studio.py backend/tests/test_lesson_markdown_write_contract.py backend/tests/test_lesson_newline_persistence.py backend/tests/test_write_path_dominance_regression.py backend/tests/test_surface_based_lesson_reads.py backend/tests/test_protected_lesson_content_surface_gate.py -q`; `.\\.venv\\Scripts\\python.exe -m pytest backend/tests/test_logs_mcp.py -q`; `rg -n "content_markdown" backend/tests/test_courses_studio.py backend/tests/test_studio_course_lessons.py backend/tests/test_lesson_markdown_write_contract.py backend/tests/test_lesson_newline_persistence.py backend/tests/test_studio_lesson_content_authority.py backend/tests/test_logs_mcp.py`; `rg -n --fixed-strings '"/studio/lessons"' backend/tests -g "*.py"`; `rg -n "If-Match|412|428|etag|ETag" backend/tests/test_courses_studio.py backend/tests/test_studio_course_lessons.py backend/tests/test_lesson_markdown_write_contract.py backend/tests/test_lesson_newline_persistence.py backend/tests/test_studio_lesson_content_authority.py`; `git diff --check`; `git status --short`.

Verification result: PASSED. Backend py_compile passed. Scoped backend lesson-content suite passed with `22 passed, 1 warning`. Full updated logs MCP backend test file passed with `5 passed, 1 warning`. `git diff --check` passed. Scans show lesson content writes now use the dedicated `/studio/lessons/{lesson_id}/content` endpoint with `If-Match`, structure responses assert absence of content authority fields, and remaining `/studio/lessons` legacy references are negative route-dominance checks rather than positive content authority paths.

Next eligible task: CCL-016. Execution stopped before CCL-016 by instruction.
