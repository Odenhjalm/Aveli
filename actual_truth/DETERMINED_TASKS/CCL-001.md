# TASK ID: CCL-001

## TITLE
Canonical course cover authoring contract

## TYPE
CONTRACT_UPDATE

## ROLE
OWNER

## DOMAIN TAG
course-cover

## SOURCE AUTHORITY
actual_truth/DETERMINED_TASKS/CCL_course_cover_lesson_content_authority_task_tree.md

## DEPENDS_ON
[]

## STATUS
DONE

## MUTATION PLANE
contracts only

## OBJECTIVE
Define the canonical course cover authoring contract. The contract must state that `app.courses.cover_media_id` is the only course-cover identity; `cover` is backend-authored read data only; media ingest and worker processing create media/runtime truth only; teacher assignment and clear use course structure authority only; frontend renders only backend-provided `cover.resolved_url`; `/api/media/cover-*` is not canonical authority. Lock the course-cover read shape as `cover = { media_id, state, resolved_url } | null`; `media_id`, `state`, and `resolved_url` are read-only backend output; `cover` is never accepted as request input; `resolved_url` must never be reconstructed by the frontend from storage paths, media IDs, filenames, buckets, or preview URLs.

## EXPECTED OUTCOME
Contract review proves no cover assignment authority exists in media pipeline, worker, frontend preview, or read composition, and proves the only frontend-authoritative cover render field is backend-provided `cover.resolved_url` inside the locked `cover` response object.

## CONSTRAINTS
Execute only after DEPENDS_ON is satisfied. Use only the declared MUTATION PLANE. Preserve source task meaning exactly. Do not create parallel authority paths, hidden fallbacks, or UI authority.

## VERIFICATION
Contract review proves no cover assignment authority exists in media pipeline, worker, frontend preview, or read composition, and proves the only frontend-authoritative cover render field is backend-provided `cover.resolved_url` inside the locked `cover` response object.

## STOP CONDITIONS
Apply the STOP CONDITIONS FOR FUTURE EXECUTION from the SOURCE AUTHORITY exactly. No task-specific override is defined here.

## EVIDENCE TO COLLECT
Current repo source evidence within the declared mutation plane; modified files if future execution mutates within the declared mutation plane; verification output required by this task; task artifact status update and execution notes.

## EXECUTION NOTES
Execution summary:
- Added course cover authoring law to the Course + Lesson Editor contract.
- Bound course cover identity to `app.courses.cover_media_id`.
- Locked `cover = { media_id, state, resolved_url } | null` as backend-authored read data.
- Forbade frontend URL reconstruction, worker cover assignment, media ingest cover assignment, `/api/media/cover-*` cover authoring authority, and local preview state as persisted truth.

Files inspected:
- actual_truth/DETERMINED_TASKS/CCL_INDEX.md
- actual_truth/DETERMINED_TASKS/CCL-001.md
- actual_truth/DETERMINED_TASKS/CCL_course_cover_lesson_content_authority_task_tree.md
- actual_truth/contracts/course_lesson_editor_contract.md
- actual_truth/contracts/COURSE_COVER_READ_CONTRACT.md
- actual_truth/contracts/SYSTEM_LAWS.md

Files changed:
- actual_truth/contracts/course_lesson_editor_contract.md
- actual_truth/DETERMINED_TASKS/CCL-001.md
- actual_truth/DETERMINED_TASKS/CCL_INDEX.md

Commands run:
- Ledger preflight and source/ledger dependency match checks.
- Contract source reads for `course_lesson_editor_contract.md`, `COURSE_COVER_READ_CONTRACT.md`, and `SYSTEM_LAWS.md`.
- `rg` verification for CCL-001 contract invariants.
- `git diff -- actual_truth/contracts/course_lesson_editor_contract.md`.

Evidence collected:
- `course_lesson_editor_contract.md` now contains `course cover identity authority: app.courses.cover_media_id`.
- `course_lesson_editor_contract.md` now contains `## 3A. COURSE COVER AUTHORING LAW`.
- `course_lesson_editor_contract.md` now contains `cover = { media_id, state, resolved_url } | null`.
- `course_lesson_editor_contract.md` now forbids `/api/media/cover-*` as course-cover authoring authority.
- `course_lesson_editor_contract.md` now requires editor frontend cover rendering only from backend-provided `cover.resolved_url`.

Verification result:
- PASS. Contract review proves the CCL-001 expected outcome is represented in the contract mutation plane.

Next eligible task(s):
- CCL-002
