# TASK ID: CCL-006

## TITLE
Course cover assignment and clear authority

## TYPE
BACKEND_ALIGNMENT

## ROLE
OWNER

## DOMAIN TAG
course-cover

## SOURCE AUTHORITY
actual_truth/DETERMINED_TASKS/CCL_course_cover_lesson_content_authority_task_tree.md

## DEPENDS_ON
[CCL-004, CCL-005]

## STATUS
DONE

## MUTATION PLANE
backend routes, schemas, services, repositories

## OBJECTIVE
Align course structure write authority for cover assignment and clear. `PATCH /studio/courses/{course_id}` must be the single mounted assignment/clear path for `cover_media_id`. The only allowed assignment input is `cover_media_id: <uuid>`; the only allowed clear input is `cover_media_id: null`; omitting `cover_media_id` means no cover change. Assignment must validate, on the backend only, that the media asset exists, is image media, is produced by the canonical media ingest pipeline, has a backend-verifiable scope or association to the same course and authorized teacher context, and is runtime-ready according to backend/runtime truth. Runtime-ready means backend read composition can emit `cover = { media_id, state, resolved_url }` with `state` ready and non-null `resolved_url`; otherwise assignment must fail closed. Reject non-image media, pending/uploaded/processing/failed media, missing runtime readiness, wrong-course media, wrong-teacher media, unscoped media, missing assets, raw URL inputs, storage path inputs, and `cover` object inputs. Clear must remove only the course pointer and must not delete media assets, storage objects, or runtime rows. Course create/update schemas and repository behavior must support set and clear without using `cover` as write authority. Worker-based assignment, frontend-driven validation as authority, and implicit assignment during ingest are forbidden.

## EXPECTED OUTCOME
Backend tests prove assign, persist, clear, no-change omission, and rejection of every invalid media case through course structure authority only; tests also prove ingest and worker paths cannot assign cover and frontend-provided validation cannot bypass backend policy.

## CONSTRAINTS
Execute only after DEPENDS_ON is satisfied. Use only the declared MUTATION PLANE. Preserve source task meaning exactly. Do not create parallel authority paths, hidden fallbacks, or UI authority.

## VERIFICATION
Backend tests prove assign, persist, clear, no-change omission, and rejection of every invalid media case through course structure authority only; tests also prove ingest and worker paths cannot assign cover and frontend-provided validation cannot bypass backend policy.

## STOP CONDITIONS
Apply the STOP CONDITIONS FOR FUTURE EXECUTION from the SOURCE AUTHORITY exactly. No task-specific override is defined here.

## EVIDENCE TO COLLECT
Current repo source evidence within the declared mutation plane; modified files if future execution mutates within the declared mutation plane; verification output required by this task; task artifact status update and execution notes.

## EXECUTION NOTES
Execution summary:
- Aligned course cover assignment and clear through course structure authority.
- `PATCH /studio/courses/{course_id}` now validates `cover_media_id` assignment server-side before persisting.
- Assignment accepts only UUID media identity, clear accepts `null`, and omitted `cover_media_id` leaves cover unchanged.
- Assignment validates image media, `course_cover` purpose, canonical source scope, ready media state, canonical derived output scope, and renderable backend public URL.
- Added a narrow `get_course_cover_pipeline_asset` repository reader so CCL-006 cover assignment reads only baseline-canonical `app.media_assets` fields instead of optional legacy streaming columns.
- Preserved the current `app.runtime_media` cover read model: source inspection and DB verification show course-cover runtime rows are projected after `app.courses.cover_media_id` is assigned, while readiness identity is stored on `app.media_assets.playback_object_path`.
- Did not modify tests because CCL-006 mutation plane is backend routes, schemas, services, and repositories; stale test expectation updates are owned by CCL-010.

Files inspected:
- actual_truth/DETERMINED_TASKS/CCL-003.md
- actual_truth/DETERMINED_TASKS/CCL-004.md
- actual_truth/DETERMINED_TASKS/CCL-006.md
- actual_truth/DETERMINED_TASKS/CCL_INDEX.md
- actual_truth/DETERMINED_TASKS/CCL_course_cover_lesson_content_authority_task_tree.md
- actual_truth/contracts/course_lesson_editor_contract.md
- backend/app/services/courses_service.py
- backend/app/repositories/media_assets.py
- backend/app/repositories/courses.py
- backend/app/repositories/runtime_media.py
- backend/app/routes/studio.py
- backend/app/schemas/__init__.py
- backend/supabase/baseline_slots/0007_media_assets_core.sql
- backend/supabase/baseline_slots/0010_worker_query_support.sql
- backend/supabase/baseline_slots/0017_runtime_media_unified.sql
- backend/tests/test_courses_studio.py
- backend/tests/test_course_cover_read_contract.py
- backend/tests/test_course_detail_view_contract.py

Files changed:
- backend/app/services/courses_service.py
- backend/app/repositories/media_assets.py
- backend/app/schemas/__init__.py
- actual_truth/DETERMINED_TASKS/CCL-006.md
- actual_truth/DETERMINED_TASKS/CCL_INDEX.md

Commands run:
- `Get-Content` inspections for CCL ledger/source authority, backend services/repositories/routes/schemas, baseline slots, and tests.
- `rg` scans for `cover_media_id`, course-cover paths, runtime media projection, legacy cover routes, and stale tests.
- `.\.venv\Scripts\python.exe -m compileall backend/app/services/courses_service.py backend/app/schemas/__init__.py backend/app/routes/studio.py backend/app/repositories/courses.py backend/app/repositories/media_assets.py backend/app/repositories/runtime_media.py`
- `git diff --check -- backend/app/services/courses_service.py backend/app/schemas/__init__.py backend/app/routes/studio.py backend/app/repositories/courses.py backend/app/repositories/media_assets.py backend/app/repositories/runtime_media.py`
- Direct CCL-006 policy validation via `.\.venv\Scripts\python.exe -`
- Direct CCL-006 DB assign/clear validation via `.\.venv\Scripts\python.exe -`
- Direct FastAPI route inventory via `.\.venv\Scripts\python.exe -`
- `.\.venv\Scripts\python.exe -m pytest backend/tests/test_course_cover_read_contract.py backend/tests/test_course_detail_view_contract.py`
- `.\.venv\Scripts\python.exe -m pytest backend/tests/test_courses_studio.py`

Evidence collected:
- `backend/app/services/courses_service.py` validates `cover_media_id` via `_validate_course_cover_assignment` before persisting assignment.
- `backend/app/services/courses_service.py` rejects invalid UUIDs, missing assets, non-image media, non-`course_cover` media, non-ready media, wrong-course source scope, missing playback output, wrong-course derived output scope, and unrenderable backend URL.
- `backend/app/services/courses_service.py` handles `cover_media_id: null` as pointer clear and omitted `cover_media_id` as no cover change.
- `backend/app/repositories/media_assets.py` exposes `get_course_cover_pipeline_asset`, which reads only canonical baseline fields from `app.media_assets` for course-cover assignment.
- `backend/app/repositories/courses.py` persists `cover_media_id` on create/update and serializes `None` as SQL null.
- `backend/app/routes/studio.py` exposes mounted `PATCH /studio/courses/{course_id}` and returns the canonical course shape after `_apply_course_read_contract`.
- Direct route inventory listed `PATCH /studio/courses/{course_id}` and `POST /api/courses/{course_id}/cover-media-assets/upload-url`; it did not list mounted `/api/media/cover-*` routes.
- Direct DB validation passed: canonical ready course-cover media assigned, persisted, rendered through backend `cover`, survived no-change omission, cleared to null, and stopped rendering after clear. It also recorded `before_runtime_rows=0`, confirming that current `app.runtime_media` projects course-cover rows after assignment from `app.courses.cover_media_id` and `app.media_assets`.
- Direct policy validation passed for valid assignment and rejection cases.
- `compileall` passed for CCL-006 backend source.
- `git diff --check` passed for CCL-006 backend source.
- `backend/tests/test_course_cover_read_contract.py` and `backend/tests/test_course_detail_view_contract.py` passed: 24 passed, 1 warning.

Stale legacy tests deferred:
- `backend/tests/test_courses_studio.py::test_studio_course_and_lesson_endpoints_follow_canonical_shape`
  - The test creates a `course_cover` media asset directly with `original_object_path=f"courses/{course_id}/covers/{cover_media_id}.png"` and `state="pending_upload"`.
  - The test then expects `PATCH /studio/courses/{course_id}` with that `cover_media_id` to return `200`.
  - CCL-006 explicitly forbids assignment of unscoped/noncanonical and non-ready media; the observed canonical response is `422` with `cover_media_id is not scoped to this course`.

Why the failing expectation is non-canonical:
- CCL-006 requires assignment to reject non-ready media and media not produced by the canonical course-cover media ingest scope.
- The failing fixture bypasses the CCL-004 canonical ingest source path `media/source/cover/courses/{course_id}/...`.
- The failing fixture leaves the asset in `pending_upload`, which CCL-006 must reject.
- Updating that expectation belongs to CCL-010 because CCL-010 is `TEST_ALIGNMENT`, has tests-only mutation plane, and explicitly owns course-cover test realignment.

Verification result:
- PASS after scoped CCL-006 verification. Source and direct DB evidence prove canonical cover assignment, persistence, no-change omission, clear, and learner/studio read composition behavior. The remaining failing studio test is stale test-alignment debt owned by CCL-010, not a CCL-006 implementation failure.

Next eligible task(s):
- CCL-007
- CCL-011
