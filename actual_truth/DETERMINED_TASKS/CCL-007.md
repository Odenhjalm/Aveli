# TASK ID: CCL-007

## TITLE
Frontend cover authoring canonical flow

## TYPE
FRONTEND_ALIGNMENT

## ROLE
OWNER

## DOMAIN TAG
course-cover

## SOURCE AUTHORITY
actual_truth/DETERMINED_TASKS/CCL_course_cover_lesson_content_authority_task_tree.md

## DEPENDS_ON
[CCL-006]

## STATUS
DONE

## MUTATION PLANE
frontend repositories, widgets, pages

## OBJECTIVE
Replace inert cover methods in `MediaPipelineRepository` and studio cover authoring callers with the canonical flow. Direct cover upload must call the canonical media ingest pipeline through the course-cover scope defined by CCL-004; completion/status must consume backend media state; assignment and clear must call the course structure update path with `cover_media_id`; lesson-media-as-cover convenience must either be removed or routed through the same contract-defined cover media identity and the same course assignment path. Frontend code may perform user guidance checks, but those checks are never authority and must not create an alternate acceptance policy, URL resolver, upload-completion flow, or implicit assignment.

## EXPECTED OUTCOME
Frontend source inspection proves no positive cover path calls inert `_unavailable()` methods or `/api/media/cover-*`.

## CONSTRAINTS
Execute only after DEPENDS_ON is satisfied. Use only the declared MUTATION PLANE. Preserve source task meaning exactly. Do not create parallel authority paths, hidden fallbacks, or UI authority.

## VERIFICATION
Frontend source inspection proves no positive cover path calls inert `_unavailable()` methods or `/api/media/cover-*`.

## STOP CONDITIONS
Apply the STOP CONDITIONS FOR FUTURE EXECUTION from the SOURCE AUTHORITY exactly. No task-specific override is defined here.

## EVIDENCE TO COLLECT
Current repo source evidence within the declared mutation plane; modified files if future execution mutates within the declared mutation plane; verification output required by this task; task artifact status update and execution notes.

## EXECUTION NOTES
Execution summary:
- Reclassified the previous CCL-007 block as an incomplete prior backend readiness/status handoff owned by CCL-004, not a true frontend task failure.
- Completed the missing mounted backend readiness/status read surface in the existing canonical media pipeline before proceeding with frontend replacement.
- Replaced positive cover frontend methods for direct cover upload, upload completion, status polling, and clear with canonical mounted backend calls.
- Kept assignment and clear under course structure authority only: ready cover status now triggers `PATCH /studio/courses/{course_id}` with `cover_media_id`, and clear uses `cover_media_id: null`.
- Removed the positive lesson-media-as-cover UI path because no canonical conversion surface exists in this task tree. The inert repository method remains unreachable from mounted frontend UI and is deferred to CCL-009/CCL-010 legacy/test cleanup.
- Did not use `/api/media/cover-*`, `/api/media/{media_id}`, upload-completion-as-readiness, frontend readiness inference, or frontend URL reconstruction.

Blocker classification:
- B/C: missing canonical backend readiness/status surface that should have existed before frontend replacement, and an incomplete CCL-004 handoff into CCL-007.
- The missing capability belongs canonically to CCL-004 because CCL-004 explicitly requires the canonical media pipeline to create `app.media_assets`, issue upload targets, confirm completion, and report media state.
- No DAG rewrite, new task ID, frontend fallback, or legacy positive route was required.

Repaired backend surface:
- `GET /api/media-assets/{media_asset_id}/status`
- Response shape: `{ media_asset_id, asset_state }`
- The response is backend-authored readiness truth only.
- The endpoint is mounted through `studio.media_pipeline_router`, reuses `_authorize_canonical_media_upload_asset`, and authorizes course-cover scope through the backend-owned canonical source path and course ownership checks.
- The endpoint does not assign, clear, or mutate `app.courses.cover_media_id`.
- The endpoint does not expose storage paths, upload URLs, signed URLs, `resolved_url`, or frontend-renderable cover data.

Files inspected:
- actual_truth/DETERMINED_TASKS/CCL-004.md
- actual_truth/DETERMINED_TASKS/CCL-005.md
- actual_truth/DETERMINED_TASKS/CCL-006.md
- actual_truth/DETERMINED_TASKS/CCL-007.md
- actual_truth/DETERMINED_TASKS/CCL_INDEX.md
- actual_truth/DETERMINED_TASKS/CCL_course_cover_lesson_content_authority_task_tree.md
- backend/app/main.py
- backend/app/routes/studio.py
- backend/app/routes/api_media.py
- backend/app/schemas/__init__.py
- frontend/lib/features/media/data/media_pipeline_repository.dart
- frontend/lib/features/studio/presentation/course_editor_page.dart
- frontend/lib/features/studio/widgets/cover_upload_card.dart
- frontend/lib/features/studio/data/studio_repository.dart
- frontend/test/widgets/cover_upload_card_test.dart
- frontend/test/widgets/course_editor_cover_polling_test.dart

Files changed:
- backend/app/routes/studio.py
- backend/app/schemas/__init__.py
- frontend/lib/features/media/data/media_pipeline_repository.dart
- frontend/lib/features/studio/presentation/course_editor_page.dart
- actual_truth/DETERMINED_TASKS/CCL-004.md
- actual_truth/DETERMINED_TASKS/CCL-007.md
- actual_truth/DETERMINED_TASKS/CCL_INDEX.md

Commands run:
- `Get-Content` inspections for ledger, source tree, backend routes/schemas, frontend repository/page/widget, and frontend tests.
- `rg` scans for `requestCoverUploadUrl`, `requestCoverFromLessonMedia`, `clearCourseCover`, `fetchStatus`, `_unavailable`, `/api/media/cover-*`, `_selectCourseCoverFromMedia`, `Använd som kursbild`, and mounted route callsites.
- `dart format frontend/lib/features/media/data/media_pipeline_repository.dart frontend/lib/features/studio/presentation/course_editor_page.dart`
- `.\.venv\Scripts\python.exe -m compileall backend/app/routes/studio.py backend/app/schemas/__init__.py`
- Direct FastAPI route inventory via `.\.venv\Scripts\python.exe -`
- Direct status response shape check via `.\.venv\Scripts\python.exe -`
- `.\.venv\Scripts\python.exe -m pytest backend/tests/test_write_path_dominance_regression.py backend/tests/test_unmounted_surface_guardrails.py`
- `.\.venv\Scripts\python.exe -m pytest backend/tests/test_studio_lesson_media_contract_unit.py backend/tests/test_media_asset_helper_alignment.py`
- `flutter analyze --no-fatal-infos lib/features/media/data/media_pipeline_repository.dart lib/features/studio/presentation/course_editor_page.dart`
- `git diff --check -- backend/app/routes/studio.py backend/app/schemas/__init__.py frontend/lib/features/media/data/media_pipeline_repository.dart frontend/lib/features/studio/presentation/course_editor_page.dart`
- `git status --short`

Evidence collected:
- Direct route inventory lists:
  - `POST /api/courses/{course_id}/cover-media-assets/upload-url`
  - `POST /api/media-assets/{media_asset_id}/upload-completion`
  - `GET /api/media-assets/{media_asset_id}/status`
  - `PATCH /studio/courses/{course_id}`
- Direct route inventory does not list mounted `/api/media/cover-*` routes.
- `backend/app/main.py` mounts `studio.media_pipeline_router` and does not mount `api_media.router`.
- `backend/app/routes/studio.py` defines `canonical_read_media_asset_status` under `media_pipeline_router` and returns only `media_asset_id` and `asset_state`.
- `backend/app/routes/studio.py` reuses `_authorize_canonical_media_upload_asset`; course-cover status authorization derives course scope from canonical backend media asset path and validates course ownership.
- Direct status response shape check returned `{'media_asset_id': '00000000-0000-0000-0000-000000000001', 'asset_state': 'ready'}`.
- `frontend/lib/features/media/data/media_pipeline_repository.dart` direct cover upload uses `POST /api/courses/{course_id}/cover-media-assets/upload-url`.
- `frontend/lib/features/media/data/media_pipeline_repository.dart` upload completion uses `POST /api/media-assets/{media_asset_id}/upload-completion`.
- `frontend/lib/features/media/data/media_pipeline_repository.dart` status polling uses `GET /api/media-assets/{media_asset_id}/status`.
- `frontend/lib/features/media/data/media_pipeline_repository.dart` clear uses `PATCH /studio/courses/{course_id}` with `cover_media_id: null`.
- `frontend/lib/features/studio/presentation/course_editor_page.dart` assigns a ready cover through `_studioRepo.updateCourse(courseId, {'cover_media_id': mediaId})`.
- `frontend/lib/features/studio/presentation/course_editor_page.dart` no longer contains `_selectCourseCoverFromMedia`, the `Använd som kursbild` positive UI action, or lesson-media leading-tap cover assignment.
- Source scan over `frontend/lib` finds `requestCoverFromLessonMedia` only as an inert repository method, with no positive mounted UI callsite.
- Source scan finds no `/api/media/cover-*` positive frontend path.
- Backend guardrail tests passed: 16 passed in `test_write_path_dominance_regression.py` and `test_unmounted_surface_guardrails.py`.
- Backend media/studio unit tests passed: 16 passed in `test_studio_lesson_media_contract_unit.py` and `test_media_asset_helper_alignment.py`.
- Backend compile passed for `backend/app/routes/studio.py` and `backend/app/schemas/__init__.py`.
- `flutter analyze --no-fatal-infos` exited 0 for the two changed frontend files; it still reported two existing nonfatal `use_build_context_synchronously` infos in `_downloadMedia`, outside the cover authoring flow.
- `git diff --check` passed for changed backend/frontend source.

Verification result:
- PASS. CCL-007 is unblocked and complete after CCL-004 backend readiness/status repair. Frontend source inspection proves the mounted positive cover authoring flow no longer calls inert `_unavailable()` methods or `/api/media/cover-*`; assignment and clear are course structure writes only; frontend does not construct cover URLs or treat readiness as frontend authority.

Next eligible task(s):
- CCL-008
- CCL-011

Next deterministic task by task ID:
- CCL-008
