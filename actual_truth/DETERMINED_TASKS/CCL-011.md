# TASK ID: CCL-011

## TITLE
Studio lesson content read backend endpoint

## TYPE
BACKEND_ALIGNMENT

## ROLE
OWNER

## DOMAIN TAG
lesson-content

## SOURCE AUTHORITY
actual_truth/DETERMINED_TASKS/CCL_course_cover_lesson_content_authority_task_tree.md

## DEPENDS_ON
[CCL-003]

## STATUS
DONE

## MUTATION PLANE
backend routes, schemas, services, repositories

## OBJECTIVE
Add the dedicated mounted studio lesson content read endpoint defined by CCL-002. The endpoint must read `app.lesson_contents.content_markdown` through backend authority, return only the locked content body `{ lesson_id, content_markdown, media }`, emit the backend-issued `ETag` concurrency token, enforce teacher/course authorization, and keep structure endpoints unchanged and content-free. `media`, if applicable, must be backend-authored governed media output only and must not expose storage paths, signed URLs, upload URLs, or frontend-resolved URLs. Existing content write behavior must remain through `PATCH /studio/lessons/{lesson_id}/content`, but it must reject writes without a matching `If-Match` token and return a stale-state failure without persisting when the token no longer matches current content authority.

## EXPECTED OUTCOME
Backend route and schema tests prove content read exists, returns exactly the locked content body plus the HTTP `ETag`, rejects stale or tokenless writes, and proves structure list/create/update responses do not contain `content_markdown`, content media, or content concurrency tokens.

## CONSTRAINTS
Execute only after DEPENDS_ON is satisfied. Use only the declared MUTATION PLANE. Preserve source task meaning exactly. Do not create parallel authority paths, hidden fallbacks, or UI authority.

## VERIFICATION
Backend route and schema tests prove content read exists, returns exactly the locked content body plus the HTTP `ETag`, rejects stale or tokenless writes, and proves structure list/create/update responses do not contain `content_markdown`, content media, or content concurrency tokens.

## STOP CONDITIONS
Apply the STOP CONDITIONS FOR FUTURE EXECUTION from the SOURCE AUTHORITY exactly. No task-specific override is defined here.

## EVIDENCE TO COLLECT
Current repo source evidence within the declared mutation plane; modified files if future execution mutates within the declared mutation plane; verification output required by this task; task artifact status update and execution notes.

## EXECUTION NOTES
Execution started.
- Dependencies verified from CCL_INDEX.md: CCL-003 is DONE.
- CCL_INDEX.md listed CCL-011 as the current eligible task.
- CCL-011 was moved to IN_PROGRESS before backend mutation.

Execution summary:
- Added the dedicated mounted studio lesson content read endpoint `GET /studio/lessons/{lesson_id}/content`.
- The read endpoint returns the locked body `{ lesson_id, content_markdown, media }` and emits the backend-issued HTTP `ETag`.
- The read endpoint reads `app.lesson_contents.content_markdown` through backend repository/service authority, treats an absent content row as backend-authored empty content for a structure-created lesson, does not mutate content, and enforces teacher course ownership.
- Added backend content read media output as governed placement metadata only: `lesson_media_id`, `media_asset_id`, `position`, `media_type`, and `state`; it does not expose storage paths, signed URLs, upload URLs, resolved URLs, filenames, or frontend-resolved fields.
- Aligned the existing content write endpoint `PATCH /studio/lessons/{lesson_id}/content` to require the current backend-issued `If-Match` token.
- Tokenless writes fail with `428 Precondition Required`; stale writes fail with `412 Precondition Failed`; successful writes emit a replacement `ETag` and return only `{ lesson_id, content_markdown }`.
- The stale-write check is repository-backed with a conditional write against the current `app.lesson_contents.content_markdown` value, so a mismatched token cannot persist content.
- Structure endpoints were not changed to include content, media, or content concurrency tokens.
- Frontend files and test files were not modified; CCL-012 and CCL-015 remain downstream alignment owners.

Files inspected:
- actual_truth/DETERMINED_TASKS/CCL_INDEX.md
- actual_truth/DETERMINED_TASKS/CCL-002.md
- actual_truth/DETERMINED_TASKS/CCL-003.md
- actual_truth/DETERMINED_TASKS/CCL-011.md
- actual_truth/DETERMINED_TASKS/CCL_course_cover_lesson_content_authority_task_tree.md
- actual_truth/contracts/course_lesson_editor_contract.md
- backend/app/main.py
- backend/app/routes/studio.py
- backend/app/repositories/courses.py
- backend/app/services/courses_service.py
- backend/app/schemas/__init__.py
- backend/app/utils/lesson_content.py
- backend/app/permissions.py
- backend/supabase/baseline_slots/0005_lesson_contents_core.sql
- backend/supabase/baseline_slots/0016_lesson_content_surface.sql
- backend/tests/conftest.py
- backend/tests/test_write_path_dominance_regression.py
- backend/tests/test_surface_based_lesson_reads.py
- backend/tests/test_protected_lesson_content_surface_gate.py
- backend/tests/test_studio_course_lessons.py
- backend/tests/test_lesson_markdown_write_contract.py

Files changed:
- backend/app/routes/studio.py
- backend/app/repositories/courses.py
- backend/app/services/courses_service.py
- backend/app/schemas/__init__.py
- actual_truth/DETERMINED_TASKS/CCL-011.md
- actual_truth/DETERMINED_TASKS/CCL_INDEX.md

Commands run:
- `rg` scans for lesson content routes, `content_markdown`, `ETag`, `If-Match`, `lesson_contents`, and affected callsites.
- `Get-Content` inspections for the CCL ledger/source authority, contract, backend route, repository, service, schema, utility, permission, baseline, and test files.
- `.\.venv\Scripts\python.exe -m py_compile backend/app/routes/studio.py backend/app/services/courses_service.py backend/app/repositories/courses.py backend/app/schemas/__init__.py`
- Direct ASGI verification script for `GET /studio/lessons/{lesson_id}/content`, `PATCH /studio/lessons/{lesson_id}/content`, ETag/If-Match behavior, ownership rejection, and content-free structure read.
- Manual local verification cleanup after the first direct script exposed unrelated local `app.cleanup_test_session` drift against missing `app.lesson_media.is_test`.
- Re-run of the direct ASGI verification script with manual cleanup.
- Direct leftover checks for CCL-011 verification users and courses.
- Direct FastAPI route inventory for studio lesson structure and content routes.
- `.\.venv\Scripts\python.exe -m pytest backend/tests/test_write_path_dominance_regression.py backend/tests/test_surface_based_lesson_reads.py backend/tests/test_protected_lesson_content_surface_gate.py -q`
- `.\.venv\Scripts\python.exe -m pytest backend/tests/test_studio_course_lessons.py::test_studio_reorder_lessons_updates_positions -q`
- `git diff --check`

Evidence collected:
- Direct route inventory lists `GET /studio/lessons/{lesson_id}/content` and `PATCH /studio/lessons/{lesson_id}/content` under the mounted studio router.
- Direct route inventory lists structure surfaces separately: `GET /studio/courses/{course_id}/lessons`, `POST /studio/courses/{course_id}/lessons`, and `PATCH /studio/lessons/{lesson_id}/structure`.
- `GET /studio/lessons/{lesson_id}/content` returned exactly the body keys `lesson_id`, `content_markdown`, and `media`.
- The content read response emitted an `ETag` beginning with `"lesson-content:`.
- A tokenless content write returned `428 Precondition Required`.
- A stale-token content write returned `412 Precondition Failed` and did not persist.
- A content write with the current `If-Match` returned `200`, persisted backend-normalized markdown, and emitted a replacement `ETag`.
- A following content read returned the updated markdown and the replacement `ETag`.
- `GET /studio/courses/{course_id}/lessons` returned only structure keys `id`, `course_id`, `lesson_title`, and `position`; it did not return `content_markdown` or `media`.
- A different teacher received `403` when reading the lesson content endpoint for a course they did not own.
- Direct cleanup checks returned `users=0` and `courses=0` for the CCL-011 verification prefixes after manual cleanup.
- Scoped backend tests passed: `backend/tests/test_write_path_dominance_regression.py`, `backend/tests/test_surface_based_lesson_reads.py`, and `backend/tests/test_protected_lesson_content_surface_gate.py` returned 11 passed, 1 warning.
- Scoped studio structure test passed: `backend/tests/test_studio_course_lessons.py::test_studio_reorder_lessons_updates_positions` returned 1 passed, 1 warning.
- `py_compile` passed for the changed backend source files.
- `git diff --check` passed.

Verification result:
- PASS. CCL-011 backend alignment is complete: the mounted dedicated lesson content read endpoint exists, returns the locked body plus HTTP `ETag`, reads from `app.lesson_contents.content_markdown`, enforces teacher/course authorization, does not mutate content, the existing write endpoint now rejects tokenless and stale writes, and structure endpoints remain content-free.

Next eligible task(s):
- CCL-012
