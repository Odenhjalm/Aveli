# TASK ID: CCL-014

## TITLE
Lesson content-in-structure legacy removal

## TYPE
LEGACY_REMOVAL

## ROLE
OWNER

## DOMAIN TAG
lesson-content

## SOURCE AUTHORITY
actual_truth/DETERMINED_TASKS/CCL_course_cover_lesson_content_authority_task_tree.md

## DEPENDS_ON
[CCL-013]

## STATUS
DONE

## MUTATION PLANE
legacy frontend/backend/test fixture cleanup

## OBJECTIVE
Remove or quarantine legacy lesson content-in-structure assumptions after the canonical hydration path exists. This includes frontend fixtures that inject `content_markdown` into `listCourseLessons`, stale `LessonStudio.contentMarkdown` usage, raw-map structure mocks with legacy `title` or `is_intro` when they mask content authority, and helper-only backend paths that make mixed lesson payloads look canonical.

## EXPECTED OUTCOME
Source scans prove `content_markdown` no longer appears in studio lesson structure models, structure-list mocks, or positive structure-read assertions.

## CONSTRAINTS
Execute only after DEPENDS_ON is satisfied. Use only the declared MUTATION PLANE. Preserve source task meaning exactly. Do not create parallel authority paths, hidden fallbacks, or UI authority.

## VERIFICATION
Source scans prove `content_markdown` no longer appears in studio lesson structure models, structure-list mocks, or positive structure-read assertions.

## STOP CONDITIONS
Apply the STOP CONDITIONS FOR FUTURE EXECUTION from the SOURCE AUTHORITY exactly. No task-specific override is defined here.

## EVIDENCE TO COLLECT
Current repo source evidence within the declared mutation plane; modified files if future execution mutates within the declared mutation plane; verification output required by this task; task artifact status update and execution notes.

## EXECUTION NOTES
STATUS set to IN_PROGRESS after verifying CCL-013 is DONE in the task ledger, CCL-011 and CCL-012 are DONE as prerequisite backend/frontend content authority evidence, and CCL_INDEX.md lists CCL-014 as the only current eligible task.

CCL-014 completed. The legacy broad frontend fixture file `frontend/test/widgets/course_editor_screen_test.dart` was quarantined because it encoded non-canonical mixed structure/content assumptions: `listCourseLessons` payloads with `content_markdown`, stale `LessonStudio.contentMarkdown`/`LessonSummary.contentMarkdown` fixture construction, legacy `is_intro` raw-map structure fixtures, and positive `upsertLesson` mocks. The file now contains only a sentinel quarantine test and no structure/content fixture path.

Backend helper-only mixed lesson paths were fail-closed without modifying backend routes or schemas: `backend/app/models.py::upsert_lesson`, `backend/app/services/courses_service.py::create_lesson`, `backend/app/services/courses_service.py::upsert_lesson`, and `backend/app/repositories/courses.py::update_lesson` now raise instead of accepting or persisting a mixed structure/content payload. The mounted studio structure routes remain on `create_lesson_structure` and `update_lesson_structure`; the mounted content route remains the dedicated content endpoint.

Files changed:
- `frontend/test/widgets/course_editor_screen_test.dart`
- `backend/app/models.py`
- `backend/app/services/courses_service.py`
- `backend/app/repositories/courses.py`
- `actual_truth/DETERMINED_TASKS/CCL-014.md`
- `actual_truth/DETERMINED_TASKS/CCL_INDEX.md`

Commands run:
- `dart format frontend/test/widgets/course_editor_screen_test.dart` from `frontend` failed because the path was repo-relative while the working directory was already `frontend`; no file mutation occurred.
- `dart format test/widgets/course_editor_screen_test.dart`
- `rg -n "content_markdown|contentMarkdown|upsertLesson\\(|listCourseLessons\\(|is_intro|LessonStudio" frontend/test/widgets/course_editor_screen_test.dart`
- `rg -n "studioRepo\\.listCourseLessons\\([^\\n]*\\)[\\s\\S]{0,200}content_markdown|content_markdown[\\s\\S]{0,200}listCourseLessons|LessonSummary\\([^\\)]*contentMarkdown|LessonStudio\\([^\\)]*contentMarkdown" frontend/test/widgets frontend/test/unit -g "*.dart"`
- `rg -n "courses_service\\.upsert_lesson|courses_service\\.create_lesson|courses_repo\\.update_lesson|models\\.upsert_lesson|await create_lesson\\(" backend/app -g "*.py"`
- `.\\.venv\\Scripts\\python.exe -m py_compile backend/app/models.py backend/app/services/courses_service.py backend/app/repositories/courses.py`
- `flutter analyze test/widgets/course_editor_screen_test.dart test/widgets/course_editor_lesson_content_lifecycle_test.dart test/unit/studio_repository_lesson_content_read_test.dart`
- `flutter test test/widgets/course_editor_screen_test.dart`
- `flutter test test/widgets/course_editor_lesson_content_lifecycle_test.dart`
- `flutter test test/unit/studio_repository_lesson_content_read_test.dart`
- `git diff --check`

Evidence collected:
- `rg` found no `content_markdown`, `contentMarkdown`, `upsertLesson`, `listCourseLessons`, `is_intro`, or `LessonStudio` references in `frontend/test/widgets/course_editor_screen_test.dart`.
- `rg` found no mixed structure/content fixture patterns in `frontend/test/widgets` or `frontend/test/unit`; remaining widget `contentMarkdown` hits are canonical content endpoint lifecycle tests or learner-course detail tests, not studio structure fixtures.
- `rg` found no active backend app callers of the fail-closed mixed helper names; active mounted studio structure code uses `courses_service.create_lesson_structure`.
- Backend py_compile passed for the modified backend files.
- Flutter analyze passed for the modified quarantine test and the canonical lesson-content frontend tests.
- Focused Flutter tests passed for the quarantine test, canonical editor lifecycle test, and studio repository lesson content read test.
- `git diff --check` passed.

Verification result:
- PASS. No positive studio structure fixture or structure-read assertion now carries `content_markdown`.
- PASS. The canonical content endpoint remains the only editor hydration source in the focused frontend lifecycle test.
- PASS. Helper-only mixed backend paths no longer persist or return mixed structure/content payloads.
- PASS. Backend routes and schemas were not modified in CCL-014.
- PASS. No CCL-015 execution was started.

Next eligible task:
- CCL-015
