# TASK ID: CCL-004

## TITLE
Course-cover ingest via canonical media pipeline

## TYPE
BACKEND_ALIGNMENT

## ROLE
OWNER

## DOMAIN TAG
course-cover

## SOURCE AUTHORITY
actual_truth/DETERMINED_TASKS/CCL_course_cover_lesson_content_authority_task_tree.md

## DEPENDS_ON
[CCL-003]

## STATUS
DONE

## MUTATION PLANE
backend routes, schemas, services, repositories

## OBJECTIVE
Reuse the canonical media ingest pipeline for course-cover ingest with explicit course-cover scope. This task must not create a second media ingest authority. If a mounted cover-scoped route is added, it is only a thin scoped adapter into the same canonical media ingest service/repository that creates `app.media_assets`, issues upload targets, confirms completion, and reports media state. Media identity creation remains owned only by the canonical media pipeline. Cover ingest must accept image assets only and must persist a backend-verifiable course/teacher scope or association required by CCL-006; that scope must not be inferred later from filenames, UI state, preview state, or client-only request context. Cover ingest must not set `app.courses.cover_media_id`, create frontend representation, fork upload-completion/status logic, write `runtime_media` directly, or bypass worker/runtime readiness. `/api/media/cover-*` must not become the positive path.

## EXPECTED OUTCOME
Backend route inspection proves there is exactly one media ingest authority, course-cover ingest delegates to the canonical media ingest pipeline, media identity is created only by the canonical pipeline, and `/api/media/cover-*` is not used as the positive path.

## CONSTRAINTS
Execute only after DEPENDS_ON is satisfied. Use only the declared MUTATION PLANE. Preserve source task meaning exactly. Do not create parallel authority paths, hidden fallbacks, or UI authority.

## VERIFICATION
Backend route inspection proves there is exactly one media ingest authority, course-cover ingest delegates to the canonical media ingest pipeline, media identity is created only by the canonical pipeline, and `/api/media/cover-*` is not used as the positive path.

## STOP CONDITIONS
Apply the STOP CONDITIONS FOR FUTURE EXECUTION from the SOURCE AUTHORITY exactly. No task-specific override is defined here.

## EVIDENCE TO COLLECT
Current repo source evidence within the declared mutation plane; modified files if future execution mutates within the declared mutation plane; verification output required by this task; task artifact status update and execution notes.

## EXECUTION NOTES
Execution summary:
- Added a mounted course-cover ingest adapter at `POST /api/courses/{course_id}/cover-media-assets/upload-url` under the existing canonical `media_pipeline_router`.
- Kept `/api/media/cover-*` out of the positive path; those routes remain unmounted legacy surfaces for CCL-009.
- Reused a single route-local canonical media upload helper for upload target issuance and `app.media_assets` creation across lesson media and course cover media.
- Extended the existing `POST /api/media-assets/{media_asset_id}/upload-completion` authority to authorize course-cover assets through backend course ownership and course-cover scope while preserving existing lesson-media behavior.
- Course-cover ingest accepts only image MIME types (`image/jpeg`, `image/png`, `image/webp`), creates only `purpose = course_cover` image assets, persists backend-generated course scope in the canonical source object path, and does not set `app.courses.cover_media_id`.

Files inspected:
- actual_truth/DETERMINED_TASKS/CCL-004.md
- actual_truth/DETERMINED_TASKS/CCL-005.md
- actual_truth/DETERMINED_TASKS/CCL-006.md
- actual_truth/DETERMINED_TASKS/CCL-009.md
- actual_truth/DETERMINED_TASKS/CCL_INDEX.md
- actual_truth/DETERMINED_TASKS/CCL_course_cover_lesson_content_authority_task_tree.md
- backend/app/main.py
- backend/app/routes/api_media.py
- backend/app/routes/studio.py
- backend/app/routes/upload.py
- backend/app/schemas/__init__.py
- backend/app/repositories/media_assets.py
- backend/app/repositories/courses.py
- backend/app/services/courses_service.py
- backend/app/utils/media_paths.py
- backend/supabase/baseline_slots/0007_media_assets_core.sql
- backend/tests/test_write_path_dominance_regression.py
- backend/tests/test_unmounted_surface_guardrails.py
- backend/tests/test_studio_lesson_media_contract_unit.py
- backend/tests/test_media_asset_helper_alignment.py

Files changed:
- backend/app/routes/studio.py
- backend/app/schemas/__init__.py
- actual_truth/DETERMINED_TASKS/CCL-004.md
- actual_truth/DETERMINED_TASKS/CCL_INDEX.md

Commands run:
- `rg` scans for media ingest, cover routes, route mounting, media scope helpers, and CCL authority text.
- `Get-Content` inspections for backend route, schema, repository, baseline, test, and task-ledger files.
- `python -m compileall backend/app/routes/studio.py backend/app/schemas/__init__.py`
- `python -m pytest backend/tests/test_write_path_dominance_regression.py backend/tests/test_unmounted_surface_guardrails.py` (system Python failed because `pytest` was not installed)
- `.\.venv\Scripts\python.exe -m pytest backend/tests/test_write_path_dominance_regression.py backend/tests/test_unmounted_surface_guardrails.py`
- `.\.venv\Scripts\python.exe -m pytest backend/tests/test_studio_lesson_media_contract_unit.py backend/tests/test_media_asset_helper_alignment.py`
- Direct app route inventory via `.\.venv\Scripts\python.exe -`
- `git diff --check -- backend/app/routes/studio.py backend/app/schemas/__init__.py`
- `git diff -- backend/app/routes/studio.py backend/app/schemas/__init__.py`
- `git status --short`

Evidence collected:
- `backend/app/main.py` mounts `studio.media_pipeline_router` but does not mount `api_media.router`.
- Direct route inventory lists:
  - `POST /api/courses/{course_id}/cover-media-assets/upload-url`
  - `POST /api/lessons/{lesson_id}/media-assets/upload-url`
  - `POST /api/media-assets/{media_asset_id}/upload-completion`
  - no `/api/media/cover-*` routes.
- `backend/app/routes/studio.py` defines `_issue_canonical_media_upload_url`, and both lesson media and course-cover upload-url routes call it.
- `backend/app/routes/studio.py` defines `_canonical_course_cover_asset_scope` and `_require_canonical_course_cover_authoring_context`, enforcing backend-owned course scope and course ownership for course-cover completion.
- `backend/app/routes/studio.py` course-cover ingest uses `media_type="image"` and `purpose="course_cover"`.
- `backend/app/routes/studio.py` course-cover ingest does not write `app.courses.cover_media_id` and does not write `runtime_media`.
- `backend/app/schemas/__init__.py` defines `CanonicalCourseCoverUploadUrlRequest` and `CanonicalCourseCoverUploadUrlResponse`.
- `.venv` pytest guardrails passed: `backend/tests/test_write_path_dominance_regression.py` and `backend/tests/test_unmounted_surface_guardrails.py` both passed, 16 total tests.
- `.venv` pytest studio/media unit checks passed: `backend/tests/test_studio_lesson_media_contract_unit.py` and `backend/tests/test_media_asset_helper_alignment.py` both passed, 16 total tests.
- `python -m compileall` passed for `backend/app/routes/studio.py` and `backend/app/schemas/__init__.py`.
- `git diff --check` reported no whitespace errors for the changed backend files.

Verification result:
- PASS. Backend route inspection proves the positive course-cover ingest path is under the canonical mounted media pipeline, media identity creation is centralized through the canonical upload helper and `media_assets_repo.create_media_asset`, completion uses the existing mounted upload-completion authority, and `/api/media/cover-*` is not mounted or used as the positive path.

Controller repair addendum for CCL-007 unblock:
- The CCL-007 block was caused by incomplete CCL-004 backend readiness/status reporting, not by a true frontend failure.
- CCL-004 already required the canonical media pipeline to create `app.media_assets`, issue upload targets, confirm completion, and report media state.
- Added the missing mounted readiness/status read surface under the same canonical media pipeline: `GET /api/media-assets/{media_asset_id}/status`.
- The status surface returns only backend-authored readiness truth: `{ media_asset_id, asset_state }`.
- The status surface reuses `_authorize_canonical_media_upload_asset`, including course-cover scope validation through the canonical backend source path and course ownership checks.
- The status surface does not assign, clear, or mutate `app.courses.cover_media_id`; does not expose storage paths, upload URLs, signed URLs, or `resolved_url`; and does not use `/api/media/cover-*` or the unmounted `api_media` router.
- Direct route inventory now lists `GET /api/media-assets/{media_asset_id}/status` together with the existing mounted upload-url and upload-completion routes.
- Direct status response shape check returned `{'media_asset_id': '00000000-0000-0000-0000-000000000001', 'asset_state': 'ready'}`.
- `.\.venv\Scripts\python.exe -m compileall backend/app/routes/studio.py backend/app/schemas/__init__.py` passed after the repair.
- `.\.venv\Scripts\python.exe -m pytest backend/tests/test_write_path_dominance_regression.py backend/tests/test_unmounted_surface_guardrails.py` passed: 16 passed.
- `.\.venv\Scripts\python.exe -m pytest backend/tests/test_studio_lesson_media_contract_unit.py backend/tests/test_media_asset_helper_alignment.py` passed: 16 passed.
- `git diff --check -- backend/app/routes/studio.py backend/app/schemas/__init__.py frontend/lib/features/media/data/media_pipeline_repository.dart frontend/lib/features/studio/presentation/course_editor_page.dart` passed.

Controller repair addendum for CCL-010 unblock:
- The CCL-010 block was classified as backend repository drift in the CCL-004-owned canonical media pipeline completion/status read surface, not as a CCL-010 test-only failure and not as a baseline-slot defect.
- Active canonical media substrate evidence is `app.media_assets.id`, `media_type`, `purpose`, `original_object_path`, `ingest_format`, `playback_object_path`, `playback_format`, and `state`; `streaming_storage_bucket`, `streaming_object_path`, and `streaming_format` are not present in the active baseline or local DB table.
- Repaired `backend/app/repositories/media_assets.py` so `get_media_asset` and `get_media_assets` read only canonical `original_*`, `ingest_*`, `playback_*`, and `state` fields from `app.media_assets`.
- Removed `streaming_storage_bucket` as a storage-access authority in `_canonical_storage_bucket_for_access`; bucket selection now derives from backend canonical `playback_object_path`, `original_object_path`, `purpose`, and `media_type`.
- No deprecated `streaming_*` columns were added to the baseline or required by the repaired positive path.
- `.\.venv\Scripts\python.exe -m py_compile backend/app/repositories/media_assets.py` passed.
- `.\.venv\Scripts\python.exe -m pytest backend/tests/test_media_asset_helper_alignment.py -q` passed: 7 passed, 1 warning.
- Direct local DB column inventory for `app.media_assets` returned only `id`, `media_type`, `purpose`, `original_object_path`, `ingest_format`, `playback_object_path`, `playback_format`, and `state`.
- Source scans found no `streaming_*` field in `backend/supabase/baseline_slots`, `backend/supabase/ACTIVE_BASELINE`, or `actual_truth/contracts`.

Next eligible task(s):
- CCL-005
- CCL-011
