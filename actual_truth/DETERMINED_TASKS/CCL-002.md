# TASK ID: CCL-002

## TITLE
Canonical studio editor lesson content read contract

## TYPE
CONTRACT_UPDATE

## ROLE
OWNER

## DOMAIN TAG
lesson-content

## SOURCE AUTHORITY
actual_truth/DETERMINED_TASKS/CCL_course_cover_lesson_content_authority_task_tree.md

## DEPENDS_ON
[]

## STATUS
DONE

## MUTATION PLANE
contracts only

## OBJECTIVE
Define the canonical studio editor lesson content read contract. The contract must add a dedicated editor content read surface for existing persisted `app.lesson_contents.content_markdown`, keep `/studio/courses/{course_id}/lessons` structure-only, and require editor hydration to use the content read surface only. Lock the content read body as `{ lesson_id, content_markdown, media }`, where `lesson_id` identifies the lesson, `content_markdown` is the backend-authored persisted markdown string, and `media` is a read-only backend-authored list of governed media objects if applicable, otherwise an empty list. The response must not include lesson structure fields such as `lesson_title`, `position`, or course structure payload. The dedicated content read must also emit the canonical concurrency token as an HTTP `ETag`; content writes must use `If-Match` with that token, and successful writes must return the backend-normalized content body plus the replacement `ETag`. Structure endpoints must never expose `content_markdown`, content media resolver fields, or content concurrency tokens.

## EXPECTED OUTCOME
Contract review proves structure reads never expose `content_markdown`, content reads never mutate structure, the content read response shape is stable, and the write surface remains the existing canonical content write guarded by the backend-issued concurrency token.

## CONSTRAINTS
Execute only after DEPENDS_ON is satisfied. Use only the declared MUTATION PLANE. Preserve source task meaning exactly. Do not create parallel authority paths, hidden fallbacks, or UI authority.

## VERIFICATION
Contract review proves structure reads never expose `content_markdown`, content reads never mutate structure, the content read response shape is stable, and the write surface remains the existing canonical content write guarded by the backend-issued concurrency token.

## STOP CONDITIONS
Apply the STOP CONDITIONS FOR FUTURE EXECUTION from the SOURCE AUTHORITY exactly. No task-specific override is defined here.

## EVIDENCE TO COLLECT
Current repo source evidence within the declared mutation plane; modified files if future execution mutates within the declared mutation plane; verification output required by this task; task artifact status update and execution notes.

## EXECUTION NOTES
Execution summary:
- Added canonical editor lesson content read contract to the Course + Lesson Editor contract.
- Defined `GET /studio/lessons/{lesson_id}/content` as the dedicated editor content read surface.
- Locked the content read body to `{ lesson_id, content_markdown, media }`.
- Defined `ETag` as the content read concurrency token and `If-Match` as required write metadata.
- Forbade content, content media, and content concurrency tokens in structure endpoints.

Files inspected:
- actual_truth/DETERMINED_TASKS/CCL_INDEX.md
- actual_truth/DETERMINED_TASKS/CCL-002.md
- actual_truth/DETERMINED_TASKS/CCL_course_cover_lesson_content_authority_task_tree.md
- actual_truth/contracts/course_lesson_editor_contract.md

Files changed:
- actual_truth/contracts/course_lesson_editor_contract.md
- actual_truth/DETERMINED_TASKS/CCL-002.md
- actual_truth/DETERMINED_TASKS/CCL_INDEX.md

Commands run:
- Contract source reads for `course_lesson_editor_contract.md`.
- `rg` verification for CCL-002 contract invariants.
- `git diff -- actual_truth/contracts/course_lesson_editor_contract.md`.

Evidence collected:
- `course_lesson_editor_contract.md` now lists `GET /studio/lessons/{lesson_id}/content`.
- `course_lesson_editor_contract.md` now contains `## 5A. CONTENT READ CONTRACT`.
- The content read response body is `{ lesson_id, content_markdown, media }`.
- The content read contract emits `ETag`.
- The content write contract requires `If-Match` and emits replacement `ETag`.
- Structure reads forbid `content_markdown`, content media, and content concurrency tokens.

Verification result:
- PASS. Contract review proves the CCL-002 expected outcome is represented in the contract mutation plane.

Next eligible task(s):
- CCL-003
