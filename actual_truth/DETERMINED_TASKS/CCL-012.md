# TASK ID: CCL-012

## TITLE
Studio frontend content read models

## TYPE
FRONTEND_ALIGNMENT

## ROLE
OWNER

## DOMAIN TAG
lesson-content

## SOURCE AUTHORITY
actual_truth/DETERMINED_TASKS/CCL_course_cover_lesson_content_authority_task_tree.md

## DEPENDS_ON
[CCL-011]

## STATUS
DONE

## MUTATION PLANE
frontend repositories and models

## OBJECTIVE
Align studio frontend repository and models with the dedicated content read. `StudioRepository` must expose a content read call that returns `lesson_id`, `content_markdown`, `media`, and the backend-issued `ETag` as transport metadata; `LessonStudio` and all structure-list models must remove `contentMarkdown`; editor content write models must remain content-only and must carry only the write payload plus the required concurrency token at the repository/transport boundary; course lesson list hydration must not infer content from structure responses.

## EXPECTED OUTCOME
Frontend unit tests prove structure models cannot carry `content_markdown` and repository content reads use the dedicated endpoint.

## CONSTRAINTS
Execute only after DEPENDS_ON is satisfied. Use only the declared MUTATION PLANE. Preserve source task meaning exactly. Do not create parallel authority paths, hidden fallbacks, or UI authority.

## VERIFICATION
Frontend unit tests prove structure models cannot carry `content_markdown` and repository content reads use the dedicated endpoint.

## STOP CONDITIONS
Apply the STOP CONDITIONS FOR FUTURE EXECUTION from the SOURCE AUTHORITY exactly. No task-specific override is defined here.

## EVIDENCE TO COLLECT
Current repo source evidence within the declared mutation plane; modified files if future execution mutates within the declared mutation plane; verification output required by this task; task artifact status update and execution notes.

## EXECUTION NOTES
STATUS set to IN_PROGRESS after verifying CCL-011 is DONE in the task ledger and CCL_INDEX.md lists CCL-012 as the only current eligible task.

Execution summary:
- Aligned the studio frontend data layer to the dedicated lesson content read surface.
- Removed `contentMarkdown` from `LessonStudio`; `LessonStudio.fromResponse` now rejects `content_markdown`, `media`, and `etag` on structure responses.
- Added content-only frontend read/write result models carrying `lesson_id`, `content_markdown`, read-only content media, and backend-issued `ETag` transport metadata.
- Added `StudioRepository.readLessonContent` for `GET /studio/lessons/{lesson_id}/content`.
- Updated `StudioRepository.updateLessonContent` to require non-empty `If-Match` and return the replacement backend `ETag`.
- Made the legacy mixed `upsertLesson` path fail closed instead of remaining a positive structure/content write path.
- Kept editor lifecycle rewrite out of scope; only minimal `course_editor_page.dart` compile alignment was made after removing `LessonStudio.contentMarkdown`.

Files changed:
- frontend/lib/features/studio/data/studio_models.dart
- frontend/lib/features/studio/data/studio_repository.dart
- frontend/lib/features/studio/presentation/course_editor_page.dart
- frontend/test/unit/studio_repository_lesson_content_read_test.dart
- actual_truth/DETERMINED_TASKS/CCL-012.md
- actual_truth/DETERMINED_TASKS/CCL_INDEX.md

Commands run:
- `dart format frontend/lib/features/studio/data/studio_models.dart frontend/lib/features/studio/data/studio_repository.dart frontend/lib/features/studio/presentation/course_editor_page.dart frontend/test/unit/studio_repository_lesson_content_read_test.dart`
- `flutter analyze lib/features/studio/data/studio_models.dart lib/features/studio/data/studio_repository.dart test/unit/studio_repository_lesson_content_read_test.dart`
- `flutter analyze lib/features/studio/presentation/course_editor_page.dart`
- `flutter test test/unit/studio_repository_lesson_content_read_test.dart`
- `git diff --check`
- scoped `rg` scans for `contentMarkdown`, `content_markdown`, `readLessonContent`, `If-Match`, and `ETag`

Evidence collected:
- `frontend/lib/features/studio/data/studio_models.dart` shows `LessonStudio` contains only `id`, `courseId`, `lessonTitle`, and `position`, and rejects `content_markdown`, `media`, and `etag` in structure payloads.
- `frontend/lib/features/studio/data/studio_models.dart` adds `StudioLessonContentRead`, `StudioLessonContentWriteResult`, and `StudioLessonContentMediaItem` as content-only models.
- `frontend/lib/features/studio/data/studio_repository.dart` reads content only from `/studio/lessons/$id/content`, requires an `ETag` header on content read/write responses, and sends `If-Match` on content write.
- `frontend/lib/features/studio/data/studio_repository.dart` makes mixed `upsertLesson` fail closed, so it is not a positive content persistence path.
- `frontend/test/unit/studio_repository_lesson_content_read_test.dart` proves structure models reject `content_markdown`, content read uses the dedicated endpoint and preserves `ETag`, content write carries `If-Match` and replacement `ETag`, and tokenless writes are rejected before transport.

Verification result:
- PASS: `flutter analyze lib/features/studio/data/studio_models.dart lib/features/studio/data/studio_repository.dart test/unit/studio_repository_lesson_content_read_test.dart`
- PASS: `flutter test test/unit/studio_repository_lesson_content_read_test.dart`
- PASS: `git diff --check`
- INFO ONLY: `flutter analyze lib/features/studio/presentation/course_editor_page.dart` still reports pre-existing `use_build_context_synchronously` infos at lines 4731 and 4741, outside the CCL-012 edits and outside the repository/model verification plane.

Next eligible task:
- CCL-013
