# TASK ID: CCL-005

## TITLE
Course-cover worker runtime-only behavior

## TYPE
BACKEND_ALIGNMENT

## ROLE
OWNER

## DOMAIN TAG
course-cover

## SOURCE AUTHORITY
actual_truth/DETERMINED_TASKS/CCL_course_cover_lesson_content_authority_task_tree.md

## DEPENDS_ON
[CCL-004]

## STATUS
DONE

## MUTATION PLANE
backend worker and media repositories

## OBJECTIVE
Align course-cover worker behavior so worker processing creates runtime truth only. The worker may transform a course-cover media asset and update canonical media/runtime readiness, but it must never assign, replace, or clear `app.courses.cover_media_id`, and must not report cover assignment as a worker side effect.

## EXPECTED OUTCOME
Worker tests and source inspection prove no worker path writes course cover assignment.

## CONSTRAINTS
Execute only after DEPENDS_ON is satisfied. Use only the declared MUTATION PLANE. Preserve source task meaning exactly. Do not create parallel authority paths, hidden fallbacks, or UI authority.

## VERIFICATION
Worker tests and source inspection prove no worker path writes course cover assignment.

## STOP CONDITIONS
Apply the STOP CONDITIONS FOR FUTURE EXECUTION from the SOURCE AUTHORITY exactly. No task-specific override is defined here.

## EVIDENCE TO COLLECT
Current repo source evidence within the declared mutation plane; modified files if future execution mutates within the declared mutation plane; verification output required by this task; task artifact status update and execution notes.

## EXECUTION NOTES
Execution summary:
- Aligned source behavior toward runtime-only worker semantics for course covers.
- Removed worker-side cover promotion reporting, cover promotion branch handling, and cover-prune side effects from `media_transcode_worker`.
- Reduced `mark_course_cover_ready_from_worker` to report only whether the canonical media readiness transition updated the media asset.
- No test files were modified because CCL-005 mutation plane is limited to backend worker and media repositories.

Files inspected:
- actual_truth/DETERMINED_TASKS/CCL-005.md
- actual_truth/DETERMINED_TASKS/CCL_INDEX.md
- backend/app/services/media_transcode_worker.py
- backend/app/repositories/media_assets.py
- backend/app/services/media_cleanup.py
- backend/app/repositories/courses.py
- backend/tests/test_media_transcode_worker_source_wait.py
- backend/tests/test_media_lifecycle_cleanup_contract.py

Files changed:
- backend/app/services/media_transcode_worker.py
- backend/app/repositories/media_assets.py
- actual_truth/DETERMINED_TASKS/CCL-005.md
- actual_truth/DETERMINED_TASKS/CCL_INDEX.md

Commands run:
- `rg` scans for `cover_applied`, `COURSE_COVER_PROMOTED`, `COURSE_COVER_READY_NOT_PROMOTED`, `prune_course_cover_assets`, `set_course_cover`, `clear_course_cover`, and `cover_media_id` in worker/repository scope.
- `Get-Content` inspections for worker, media repository, course repository, media cleanup, and worker tests.
- `python -m compileall backend/app/services/media_transcode_worker.py backend/app/repositories/media_assets.py`
- `.\.venv\Scripts\python.exe -m pytest backend/tests/test_media_transcode_worker_source_wait.py`
- `git diff --check -- backend/app/services/media_transcode_worker.py backend/app/repositories/media_assets.py`
- `git status --short`

Evidence collected:
- `backend/app/services/media_transcode_worker.py` no longer imports `media_cleanup`.
- `backend/app/services/media_transcode_worker.py` no longer contains `cover_applied`, `COURSE_COVER_PROMOTED`, `COURSE_COVER_READY_NOT_PROMOTED`, `prune_course_cover_assets`, `set_course_cover`, `clear_course_cover`, or `cover_media_id`.
- `backend/app/repositories/media_assets.py` no longer contains `cover_applied`, assignment IDs, course ID reporting, or `cover_media_id` in `mark_course_cover_ready_from_worker`.
- `python -m compileall` passed for worker and media repository files.
- `git diff --check` passed for worker and media repository files.

Repaired blocker classification:
- Stale legacy-test conflict caused by verification scope reaching ahead of downstream TEST_ALIGNMENT responsibility.
- Not a true CCL-005 implementation failure against canonical authority.
- Not a graph defect.
- Not a reason to reintroduce worker assignment or promotion semantics.

Stale legacy tests deferred:
- `backend/tests/test_media_transcode_worker_source_wait.py::test_transcode_cover_promotes_without_legacy_public_url`
  - `test_transcode_cover_promotes_without_legacy_public_url` attempts to patch `worker.media_cleanup.prune_course_cover_assets` and expects `COURSE_COVER_PROMOTED`.
- `backend/tests/test_media_transcode_worker_source_wait.py::test_transcode_cover_logs_when_ready_asset_not_promoted`
  - `test_transcode_cover_logs_when_ready_asset_not_promoted` expects `COURSE_COVER_READY_NOT_PROMOTED`.

Why the failing expectations are non-canonical:
- CCL-005 explicitly requires worker processing to create runtime/media readiness truth only.
- CCL-005 explicitly forbids worker assignment, replacement, clear, and cover assignment reporting as a worker side effect.
- `COURSE_COVER_PROMOTED`, `COURSE_COVER_READY_NOT_PROMOTED`, `cover_applied`, and worker-side cover pruning encode the removed worker-promotion model.
- Satisfying those tests would require reintroducing forbidden worker cover-promotion semantics.

Ownership of deferred alignment:
- CCL-005 owns backend worker and media repository source behavior only.
- CCL-009 owns later route/source inventory proving no active or positive path treats worker cover promotion as canonical.
- CCL-010 owns test alignment because it is `TEST_ALIGNMENT`, has `tests only` mutation plane, and explicitly requires tests to prove worker no-assignment behavior and absence of `/api/media/cover-*` positive paths.

Exact failed verification:
- `.\.venv\Scripts\python.exe -m pytest backend/tests/test_media_transcode_worker_source_wait.py`
- Result: 3 passed, 2 failed.
- Failed tests:
  - `backend/tests/test_media_transcode_worker_source_wait.py::test_transcode_cover_promotes_without_legacy_public_url`
  - `backend/tests/test_media_transcode_worker_source_wait.py::test_transcode_cover_logs_when_ready_asset_not_promoted`

Corrected canonical verification scope:
- Source inspection must prove worker creates runtime/media readiness truth only.
- Source inspection must prove worker does not assign, replace, or clear `app.courses.cover_media_id`.
- Source inspection must prove worker no longer emits or depends on forbidden promotion semantics.
- Changed backend worker/repository source must compile.
- CCL-005-specific non-legacy worker tests must pass, with stale legacy-promotion assertions explicitly deferred to CCL-010.

Final verification evidence:
- `rg` over `backend/app/services/media_transcode_worker.py` and `backend/app/repositories/media_assets.py` for `cover_applied`, `COURSE_COVER_PROMOTED`, `COURSE_COVER_READY_NOT_PROMOTED`, `prune_course_cover_assets`, `set_course_cover`, `clear_course_cover`, `cover_media_id`, and direct `app.courses` write SQL returned no matches.
- `.\.venv\Scripts\python.exe -m compileall backend/app/services/media_transcode_worker.py backend/app/repositories/media_assets.py` passed.
- `.\.venv\Scripts\python.exe -m pytest backend/tests/test_media_transcode_worker_source_wait.py -k "not promotes_without_legacy_public_url and not logs_when_ready_asset_not_promoted"` passed: 3 passed, 2 deselected.
- `git diff --check -- backend/app/services/media_transcode_worker.py backend/app/repositories/media_assets.py actual_truth/DETERMINED_TASKS/CCL-005.md actual_truth/DETERMINED_TASKS/CCL_INDEX.md` passed.

Verification result:
- PASS after deterministic controller repair. CCL-005 source behavior satisfies runtime-only worker authority; stale legacy test expectations are recorded as deferred course-cover test-alignment debt owned by CCL-010.

Next eligible task(s):
- CCL-006
- CCL-011
