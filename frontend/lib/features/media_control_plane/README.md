# Media Control Plane

This directory is the isolated frontend workspace for the future Aveli Media
Control Plane admin surface.

It currently contains scaffolding only so future work can add control-plane UI
without changing the current lesson playback, upload, or rendering behavior.
