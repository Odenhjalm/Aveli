# Archived Legacy Snapshot

STATUS: ARCHIVED_LEGACY_NON_AUTHORITATIVE

This directory name is historical. It is not active database baseline authority.
It is preserved only as read-only legacy evidence from a superseded baseline
snapshot.

Canonical database baseline authority is:

- `backend/supabase/baseline_v2_slots/`
- `backend/supabase/baseline_v2_slots.lock.json`

Do not replay, auto-discover, or verify this directory as canonical structure.
